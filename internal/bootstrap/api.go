package bootstrap

// 注册Api
func InitApiHandler() {
	//initApi(internal.GetRouter())
}

// 注册Api
func initApi(/*router *gin.Engine*/) {
	/*var (
		controller = handler.GetApiController()
	)
	// 获取直播间列表
	router.GET("/channels", controller.GetOnlineChannels)
	// 刷新直播间列表
	router.GET("/channels/check", controller.PostDispatchJob)
	// 获取直播间用户列表
	router.GET("/channel/user/lists", controller.GetChannelUsers)
	// 停止录制直播
	router.POST("/recording/stop", controller.PostStopRecording)
	// 添加录制直播
	router.POST("/recording/channels", controller.PostRecordingShowid)*/
}
