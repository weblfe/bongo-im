// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// CMFAuthAccess is an object representing the database table.
type CMFAuthAccess struct {
	ID       uint64 `boil:"id" json:"id" toml:"id" yaml:"id"`
	RoleID   uint   `boil:"role_id" json:"role_id" toml:"role_id" yaml:"role_id"`
	RuleName string `boil:"rule_name" json:"rule_name" toml:"rule_name" yaml:"rule_name"`
	Type     string `boil:"type" json:"type" toml:"type" yaml:"type"`

	R *cmfAuthAccessR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L cmfAuthAccessL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CMFAuthAccessColumns = struct {
	ID       string
	RoleID   string
	RuleName string
	Type     string
}{
	ID:       "id",
	RoleID:   "role_id",
	RuleName: "rule_name",
	Type:     "type",
}

// Generated where

var CMFAuthAccessWhere = struct {
	ID       whereHelperuint64
	RoleID   whereHelperuint
	RuleName whereHelperstring
	Type     whereHelperstring
}{
	ID:       whereHelperuint64{field: "`cmf_auth_access`.`id`"},
	RoleID:   whereHelperuint{field: "`cmf_auth_access`.`role_id`"},
	RuleName: whereHelperstring{field: "`cmf_auth_access`.`rule_name`"},
	Type:     whereHelperstring{field: "`cmf_auth_access`.`type`"},
}

// CMFAuthAccessRels is where relationship names are stored.
var CMFAuthAccessRels = struct {
}{}

// cmfAuthAccessR is where relationships are stored.
type cmfAuthAccessR struct {
}

// NewStruct creates a new relationship struct
func (*cmfAuthAccessR) NewStruct() *cmfAuthAccessR {
	return &cmfAuthAccessR{}
}

// cmfAuthAccessL is where Load methods for each relationship are stored.
type cmfAuthAccessL struct{}

var (
	cmfAuthAccessAllColumns            = []string{"id", "role_id", "rule_name", "type"}
	cmfAuthAccessColumnsWithoutDefault = []string{"role_id", "rule_name", "type"}
	cmfAuthAccessColumnsWithDefault    = []string{"id"}
	cmfAuthAccessPrimaryKeyColumns     = []string{"id"}
)

type (
	// CMFAuthAccessSlice is an alias for a slice of pointers to CMFAuthAccess.
	// This should generally be used opposed to []CMFAuthAccess.
	CMFAuthAccessSlice []*CMFAuthAccess
	// CMFAuthAccessHook is the signature for custom CMFAuthAccess hook methods
	CMFAuthAccessHook func(context.Context, boil.ContextExecutor, *CMFAuthAccess) error

	cmfAuthAccessQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	cmfAuthAccessType                 = reflect.TypeOf(&CMFAuthAccess{})
	cmfAuthAccessMapping              = queries.MakeStructMapping(cmfAuthAccessType)
	cmfAuthAccessPrimaryKeyMapping, _ = queries.BindMapping(cmfAuthAccessType, cmfAuthAccessMapping, cmfAuthAccessPrimaryKeyColumns)
	cmfAuthAccessInsertCacheMut       sync.RWMutex
	cmfAuthAccessInsertCache          = make(map[string]insertCache)
	cmfAuthAccessUpdateCacheMut       sync.RWMutex
	cmfAuthAccessUpdateCache          = make(map[string]updateCache)
	cmfAuthAccessUpsertCacheMut       sync.RWMutex
	cmfAuthAccessUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var cmfAuthAccessBeforeInsertHooks []CMFAuthAccessHook
var cmfAuthAccessBeforeUpdateHooks []CMFAuthAccessHook
var cmfAuthAccessBeforeDeleteHooks []CMFAuthAccessHook
var cmfAuthAccessBeforeUpsertHooks []CMFAuthAccessHook

var cmfAuthAccessAfterInsertHooks []CMFAuthAccessHook
var cmfAuthAccessAfterSelectHooks []CMFAuthAccessHook
var cmfAuthAccessAfterUpdateHooks []CMFAuthAccessHook
var cmfAuthAccessAfterDeleteHooks []CMFAuthAccessHook
var cmfAuthAccessAfterUpsertHooks []CMFAuthAccessHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CMFAuthAccess) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfAuthAccessBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CMFAuthAccess) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfAuthAccessBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CMFAuthAccess) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfAuthAccessBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CMFAuthAccess) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfAuthAccessBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CMFAuthAccess) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfAuthAccessAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CMFAuthAccess) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfAuthAccessAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CMFAuthAccess) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfAuthAccessAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CMFAuthAccess) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfAuthAccessAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CMFAuthAccess) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfAuthAccessAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCMFAuthAccessHook registers your hook function for all future operations.
func AddCMFAuthAccessHook(hookPoint boil.HookPoint, cmfAuthAccessHook CMFAuthAccessHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		cmfAuthAccessBeforeInsertHooks = append(cmfAuthAccessBeforeInsertHooks, cmfAuthAccessHook)
	case boil.BeforeUpdateHook:
		cmfAuthAccessBeforeUpdateHooks = append(cmfAuthAccessBeforeUpdateHooks, cmfAuthAccessHook)
	case boil.BeforeDeleteHook:
		cmfAuthAccessBeforeDeleteHooks = append(cmfAuthAccessBeforeDeleteHooks, cmfAuthAccessHook)
	case boil.BeforeUpsertHook:
		cmfAuthAccessBeforeUpsertHooks = append(cmfAuthAccessBeforeUpsertHooks, cmfAuthAccessHook)
	case boil.AfterInsertHook:
		cmfAuthAccessAfterInsertHooks = append(cmfAuthAccessAfterInsertHooks, cmfAuthAccessHook)
	case boil.AfterSelectHook:
		cmfAuthAccessAfterSelectHooks = append(cmfAuthAccessAfterSelectHooks, cmfAuthAccessHook)
	case boil.AfterUpdateHook:
		cmfAuthAccessAfterUpdateHooks = append(cmfAuthAccessAfterUpdateHooks, cmfAuthAccessHook)
	case boil.AfterDeleteHook:
		cmfAuthAccessAfterDeleteHooks = append(cmfAuthAccessAfterDeleteHooks, cmfAuthAccessHook)
	case boil.AfterUpsertHook:
		cmfAuthAccessAfterUpsertHooks = append(cmfAuthAccessAfterUpsertHooks, cmfAuthAccessHook)
	}
}

// One returns a single cmfAuthAccess record from the query.
func (q cmfAuthAccessQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CMFAuthAccess, error) {
	o := &CMFAuthAccess{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for cmf_auth_access")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all CMFAuthAccess records from the query.
func (q cmfAuthAccessQuery) All(ctx context.Context, exec boil.ContextExecutor) (CMFAuthAccessSlice, error) {
	var o []*CMFAuthAccess

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CMFAuthAccess slice")
	}

	if len(cmfAuthAccessAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all CMFAuthAccess records in the query.
func (q cmfAuthAccessQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count cmf_auth_access rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q cmfAuthAccessQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if cmf_auth_access exists")
	}

	return count > 0, nil
}

// CMFAuthAccesses retrieves all the records using an executor.
func CMFAuthAccesses(mods ...qm.QueryMod) cmfAuthAccessQuery {
	mods = append(mods, qm.From("`cmf_auth_access`"))
	return cmfAuthAccessQuery{NewQuery(mods...)}
}

// FindCMFAuthAccess retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCMFAuthAccess(ctx context.Context, exec boil.ContextExecutor, iD uint64, selectCols ...string) (*CMFAuthAccess, error) {
	cmfAuthAccessObj := &CMFAuthAccess{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `cmf_auth_access` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, cmfAuthAccessObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from cmf_auth_access")
	}

	return cmfAuthAccessObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CMFAuthAccess) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cmf_auth_access provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(cmfAuthAccessColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	cmfAuthAccessInsertCacheMut.RLock()
	cache, cached := cmfAuthAccessInsertCache[key]
	cmfAuthAccessInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			cmfAuthAccessAllColumns,
			cmfAuthAccessColumnsWithDefault,
			cmfAuthAccessColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(cmfAuthAccessType, cmfAuthAccessMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(cmfAuthAccessType, cmfAuthAccessMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `cmf_auth_access` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `cmf_auth_access` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `cmf_auth_access` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, cmfAuthAccessPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into cmf_auth_access")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == cmfAuthAccessMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cmf_auth_access")
	}

CacheNoHooks:
	if !cached {
		cmfAuthAccessInsertCacheMut.Lock()
		cmfAuthAccessInsertCache[key] = cache
		cmfAuthAccessInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the CMFAuthAccess.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CMFAuthAccess) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	cmfAuthAccessUpdateCacheMut.RLock()
	cache, cached := cmfAuthAccessUpdateCache[key]
	cmfAuthAccessUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			cmfAuthAccessAllColumns,
			cmfAuthAccessPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update cmf_auth_access, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `cmf_auth_access` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, cmfAuthAccessPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(cmfAuthAccessType, cmfAuthAccessMapping, append(wl, cmfAuthAccessPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update cmf_auth_access row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for cmf_auth_access")
	}

	if !cached {
		cmfAuthAccessUpdateCacheMut.Lock()
		cmfAuthAccessUpdateCache[key] = cache
		cmfAuthAccessUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q cmfAuthAccessQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for cmf_auth_access")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for cmf_auth_access")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CMFAuthAccessSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfAuthAccessPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `cmf_auth_access` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfAuthAccessPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in cmfAuthAccess slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all cmfAuthAccess")
	}
	return rowsAff, nil
}

var mySQLCMFAuthAccessUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CMFAuthAccess) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cmf_auth_access provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(cmfAuthAccessColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLCMFAuthAccessUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	cmfAuthAccessUpsertCacheMut.RLock()
	cache, cached := cmfAuthAccessUpsertCache[key]
	cmfAuthAccessUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			cmfAuthAccessAllColumns,
			cmfAuthAccessColumnsWithDefault,
			cmfAuthAccessColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			cmfAuthAccessAllColumns,
			cmfAuthAccessPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert cmf_auth_access, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`cmf_auth_access`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `cmf_auth_access` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(cmfAuthAccessType, cmfAuthAccessMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(cmfAuthAccessType, cmfAuthAccessMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for cmf_auth_access")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == cmfAuthAccessMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(cmfAuthAccessType, cmfAuthAccessMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for cmf_auth_access")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cmf_auth_access")
	}

CacheNoHooks:
	if !cached {
		cmfAuthAccessUpsertCacheMut.Lock()
		cmfAuthAccessUpsertCache[key] = cache
		cmfAuthAccessUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single CMFAuthAccess record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CMFAuthAccess) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CMFAuthAccess provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cmfAuthAccessPrimaryKeyMapping)
	sql := "DELETE FROM `cmf_auth_access` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from cmf_auth_access")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for cmf_auth_access")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q cmfAuthAccessQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no cmfAuthAccessQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cmf_auth_access")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cmf_auth_access")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CMFAuthAccessSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(cmfAuthAccessBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfAuthAccessPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `cmf_auth_access` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfAuthAccessPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cmfAuthAccess slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cmf_auth_access")
	}

	if len(cmfAuthAccessAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CMFAuthAccess) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCMFAuthAccess(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CMFAuthAccessSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CMFAuthAccessSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfAuthAccessPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `cmf_auth_access`.* FROM `cmf_auth_access` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfAuthAccessPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CMFAuthAccessSlice")
	}

	*o = slice

	return nil
}

// CMFAuthAccessExists checks if the CMFAuthAccess row exists.
func CMFAuthAccessExists(ctx context.Context, exec boil.ContextExecutor, iD uint64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `cmf_auth_access` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if cmf_auth_access exists")
	}

	return exists, nil
}
