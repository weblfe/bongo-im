// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// CMFAuthRule is an object representing the database table.
type CMFAuthRule struct {
	ID        uint   `boil:"id" json:"id" toml:"id" yaml:"id"`
	Status    uint8  `boil:"status" json:"status" toml:"status" yaml:"status"`
	App       string `boil:"app" json:"app" toml:"app" yaml:"app"`
	Type      string `boil:"type" json:"type" toml:"type" yaml:"type"`
	Name      string `boil:"name" json:"name" toml:"name" yaml:"name"`
	Param     string `boil:"param" json:"param" toml:"param" yaml:"param"`
	Title     string `boil:"title" json:"title" toml:"title" yaml:"title"`
	Condition string `boil:"condition" json:"condition" toml:"condition" yaml:"condition"`

	R *cmfAuthRuleR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L cmfAuthRuleL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CMFAuthRuleColumns = struct {
	ID        string
	Status    string
	App       string
	Type      string
	Name      string
	Param     string
	Title     string
	Condition string
}{
	ID:        "id",
	Status:    "status",
	App:       "app",
	Type:      "type",
	Name:      "name",
	Param:     "param",
	Title:     "title",
	Condition: "condition",
}

// Generated where

var CMFAuthRuleWhere = struct {
	ID        whereHelperuint
	Status    whereHelperuint8
	App       whereHelperstring
	Type      whereHelperstring
	Name      whereHelperstring
	Param     whereHelperstring
	Title     whereHelperstring
	Condition whereHelperstring
}{
	ID:        whereHelperuint{field: "`cmf_auth_rule`.`id`"},
	Status:    whereHelperuint8{field: "`cmf_auth_rule`.`status`"},
	App:       whereHelperstring{field: "`cmf_auth_rule`.`app`"},
	Type:      whereHelperstring{field: "`cmf_auth_rule`.`type`"},
	Name:      whereHelperstring{field: "`cmf_auth_rule`.`name`"},
	Param:     whereHelperstring{field: "`cmf_auth_rule`.`param`"},
	Title:     whereHelperstring{field: "`cmf_auth_rule`.`title`"},
	Condition: whereHelperstring{field: "`cmf_auth_rule`.`condition`"},
}

// CMFAuthRuleRels is where relationship names are stored.
var CMFAuthRuleRels = struct {
}{}

// cmfAuthRuleR is where relationships are stored.
type cmfAuthRuleR struct {
}

// NewStruct creates a new relationship struct
func (*cmfAuthRuleR) NewStruct() *cmfAuthRuleR {
	return &cmfAuthRuleR{}
}

// cmfAuthRuleL is where Load methods for each relationship are stored.
type cmfAuthRuleL struct{}

var (
	cmfAuthRuleAllColumns            = []string{"id", "status", "app", "type", "name", "param", "title", "condition"}
	cmfAuthRuleColumnsWithoutDefault = []string{"app", "type", "name", "param", "title", "condition"}
	cmfAuthRuleColumnsWithDefault    = []string{"id", "status"}
	cmfAuthRulePrimaryKeyColumns     = []string{"id"}
)

type (
	// CMFAuthRuleSlice is an alias for a slice of pointers to CMFAuthRule.
	// This should generally be used opposed to []CMFAuthRule.
	CMFAuthRuleSlice []*CMFAuthRule
	// CMFAuthRuleHook is the signature for custom CMFAuthRule hook methods
	CMFAuthRuleHook func(context.Context, boil.ContextExecutor, *CMFAuthRule) error

	cmfAuthRuleQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	cmfAuthRuleType                 = reflect.TypeOf(&CMFAuthRule{})
	cmfAuthRuleMapping              = queries.MakeStructMapping(cmfAuthRuleType)
	cmfAuthRulePrimaryKeyMapping, _ = queries.BindMapping(cmfAuthRuleType, cmfAuthRuleMapping, cmfAuthRulePrimaryKeyColumns)
	cmfAuthRuleInsertCacheMut       sync.RWMutex
	cmfAuthRuleInsertCache          = make(map[string]insertCache)
	cmfAuthRuleUpdateCacheMut       sync.RWMutex
	cmfAuthRuleUpdateCache          = make(map[string]updateCache)
	cmfAuthRuleUpsertCacheMut       sync.RWMutex
	cmfAuthRuleUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var cmfAuthRuleBeforeInsertHooks []CMFAuthRuleHook
var cmfAuthRuleBeforeUpdateHooks []CMFAuthRuleHook
var cmfAuthRuleBeforeDeleteHooks []CMFAuthRuleHook
var cmfAuthRuleBeforeUpsertHooks []CMFAuthRuleHook

var cmfAuthRuleAfterInsertHooks []CMFAuthRuleHook
var cmfAuthRuleAfterSelectHooks []CMFAuthRuleHook
var cmfAuthRuleAfterUpdateHooks []CMFAuthRuleHook
var cmfAuthRuleAfterDeleteHooks []CMFAuthRuleHook
var cmfAuthRuleAfterUpsertHooks []CMFAuthRuleHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CMFAuthRule) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfAuthRuleBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CMFAuthRule) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfAuthRuleBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CMFAuthRule) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfAuthRuleBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CMFAuthRule) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfAuthRuleBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CMFAuthRule) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfAuthRuleAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CMFAuthRule) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfAuthRuleAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CMFAuthRule) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfAuthRuleAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CMFAuthRule) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfAuthRuleAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CMFAuthRule) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfAuthRuleAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCMFAuthRuleHook registers your hook function for all future operations.
func AddCMFAuthRuleHook(hookPoint boil.HookPoint, cmfAuthRuleHook CMFAuthRuleHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		cmfAuthRuleBeforeInsertHooks = append(cmfAuthRuleBeforeInsertHooks, cmfAuthRuleHook)
	case boil.BeforeUpdateHook:
		cmfAuthRuleBeforeUpdateHooks = append(cmfAuthRuleBeforeUpdateHooks, cmfAuthRuleHook)
	case boil.BeforeDeleteHook:
		cmfAuthRuleBeforeDeleteHooks = append(cmfAuthRuleBeforeDeleteHooks, cmfAuthRuleHook)
	case boil.BeforeUpsertHook:
		cmfAuthRuleBeforeUpsertHooks = append(cmfAuthRuleBeforeUpsertHooks, cmfAuthRuleHook)
	case boil.AfterInsertHook:
		cmfAuthRuleAfterInsertHooks = append(cmfAuthRuleAfterInsertHooks, cmfAuthRuleHook)
	case boil.AfterSelectHook:
		cmfAuthRuleAfterSelectHooks = append(cmfAuthRuleAfterSelectHooks, cmfAuthRuleHook)
	case boil.AfterUpdateHook:
		cmfAuthRuleAfterUpdateHooks = append(cmfAuthRuleAfterUpdateHooks, cmfAuthRuleHook)
	case boil.AfterDeleteHook:
		cmfAuthRuleAfterDeleteHooks = append(cmfAuthRuleAfterDeleteHooks, cmfAuthRuleHook)
	case boil.AfterUpsertHook:
		cmfAuthRuleAfterUpsertHooks = append(cmfAuthRuleAfterUpsertHooks, cmfAuthRuleHook)
	}
}

// One returns a single cmfAuthRule record from the query.
func (q cmfAuthRuleQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CMFAuthRule, error) {
	o := &CMFAuthRule{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for cmf_auth_rule")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all CMFAuthRule records from the query.
func (q cmfAuthRuleQuery) All(ctx context.Context, exec boil.ContextExecutor) (CMFAuthRuleSlice, error) {
	var o []*CMFAuthRule

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CMFAuthRule slice")
	}

	if len(cmfAuthRuleAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all CMFAuthRule records in the query.
func (q cmfAuthRuleQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count cmf_auth_rule rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q cmfAuthRuleQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if cmf_auth_rule exists")
	}

	return count > 0, nil
}

// CMFAuthRules retrieves all the records using an executor.
func CMFAuthRules(mods ...qm.QueryMod) cmfAuthRuleQuery {
	mods = append(mods, qm.From("`cmf_auth_rule`"))
	return cmfAuthRuleQuery{NewQuery(mods...)}
}

// FindCMFAuthRule retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCMFAuthRule(ctx context.Context, exec boil.ContextExecutor, iD uint, selectCols ...string) (*CMFAuthRule, error) {
	cmfAuthRuleObj := &CMFAuthRule{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `cmf_auth_rule` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, cmfAuthRuleObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from cmf_auth_rule")
	}

	return cmfAuthRuleObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CMFAuthRule) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cmf_auth_rule provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(cmfAuthRuleColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	cmfAuthRuleInsertCacheMut.RLock()
	cache, cached := cmfAuthRuleInsertCache[key]
	cmfAuthRuleInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			cmfAuthRuleAllColumns,
			cmfAuthRuleColumnsWithDefault,
			cmfAuthRuleColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(cmfAuthRuleType, cmfAuthRuleMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(cmfAuthRuleType, cmfAuthRuleMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `cmf_auth_rule` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `cmf_auth_rule` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `cmf_auth_rule` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, cmfAuthRulePrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into cmf_auth_rule")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == cmfAuthRuleMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cmf_auth_rule")
	}

CacheNoHooks:
	if !cached {
		cmfAuthRuleInsertCacheMut.Lock()
		cmfAuthRuleInsertCache[key] = cache
		cmfAuthRuleInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the CMFAuthRule.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CMFAuthRule) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	cmfAuthRuleUpdateCacheMut.RLock()
	cache, cached := cmfAuthRuleUpdateCache[key]
	cmfAuthRuleUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			cmfAuthRuleAllColumns,
			cmfAuthRulePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update cmf_auth_rule, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `cmf_auth_rule` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, cmfAuthRulePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(cmfAuthRuleType, cmfAuthRuleMapping, append(wl, cmfAuthRulePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update cmf_auth_rule row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for cmf_auth_rule")
	}

	if !cached {
		cmfAuthRuleUpdateCacheMut.Lock()
		cmfAuthRuleUpdateCache[key] = cache
		cmfAuthRuleUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q cmfAuthRuleQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for cmf_auth_rule")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for cmf_auth_rule")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CMFAuthRuleSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfAuthRulePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `cmf_auth_rule` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfAuthRulePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in cmfAuthRule slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all cmfAuthRule")
	}
	return rowsAff, nil
}

var mySQLCMFAuthRuleUniqueColumns = []string{
	"id",
	"name",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CMFAuthRule) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cmf_auth_rule provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(cmfAuthRuleColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLCMFAuthRuleUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	cmfAuthRuleUpsertCacheMut.RLock()
	cache, cached := cmfAuthRuleUpsertCache[key]
	cmfAuthRuleUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			cmfAuthRuleAllColumns,
			cmfAuthRuleColumnsWithDefault,
			cmfAuthRuleColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			cmfAuthRuleAllColumns,
			cmfAuthRulePrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert cmf_auth_rule, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`cmf_auth_rule`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `cmf_auth_rule` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(cmfAuthRuleType, cmfAuthRuleMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(cmfAuthRuleType, cmfAuthRuleMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for cmf_auth_rule")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == cmfAuthRuleMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(cmfAuthRuleType, cmfAuthRuleMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for cmf_auth_rule")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cmf_auth_rule")
	}

CacheNoHooks:
	if !cached {
		cmfAuthRuleUpsertCacheMut.Lock()
		cmfAuthRuleUpsertCache[key] = cache
		cmfAuthRuleUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single CMFAuthRule record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CMFAuthRule) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CMFAuthRule provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cmfAuthRulePrimaryKeyMapping)
	sql := "DELETE FROM `cmf_auth_rule` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from cmf_auth_rule")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for cmf_auth_rule")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q cmfAuthRuleQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no cmfAuthRuleQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cmf_auth_rule")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cmf_auth_rule")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CMFAuthRuleSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(cmfAuthRuleBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfAuthRulePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `cmf_auth_rule` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfAuthRulePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cmfAuthRule slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cmf_auth_rule")
	}

	if len(cmfAuthRuleAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CMFAuthRule) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCMFAuthRule(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CMFAuthRuleSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CMFAuthRuleSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfAuthRulePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `cmf_auth_rule`.* FROM `cmf_auth_rule` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfAuthRulePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CMFAuthRuleSlice")
	}

	*o = slice

	return nil
}

// CMFAuthRuleExists checks if the CMFAuthRule row exists.
func CMFAuthRuleExists(ctx context.Context, exec boil.ContextExecutor, iD uint) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `cmf_auth_rule` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if cmf_auth_rule exists")
	}

	return exists, nil
}
