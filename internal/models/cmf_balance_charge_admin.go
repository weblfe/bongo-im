// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// CMFBalanceChargeAdmin is an object representing the database table.
type CMFBalanceChargeAdmin struct {
	ID      uint          `boil:"id" json:"id" toml:"id" yaml:"id"`
	Touid   int           `boil:"touid" json:"touid" toml:"touid" yaml:"touid"`
	Balance types.Decimal `boil:"balance" json:"balance" toml:"balance" yaml:"balance"`
	Addtime int           `boil:"addtime" json:"addtime" toml:"addtime" yaml:"addtime"`
	Admin   string        `boil:"admin" json:"admin" toml:"admin" yaml:"admin"`
	IP      string        `boil:"ip" json:"ip" toml:"ip" yaml:"ip"`

	R *cmfBalanceChargeAdminR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L cmfBalanceChargeAdminL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CMFBalanceChargeAdminColumns = struct {
	ID      string
	Touid   string
	Balance string
	Addtime string
	Admin   string
	IP      string
}{
	ID:      "id",
	Touid:   "touid",
	Balance: "balance",
	Addtime: "addtime",
	Admin:   "admin",
	IP:      "ip",
}

// Generated where

var CMFBalanceChargeAdminWhere = struct {
	ID      whereHelperuint
	Touid   whereHelperint
	Balance whereHelpertypes_Decimal
	Addtime whereHelperint
	Admin   whereHelperstring
	IP      whereHelperstring
}{
	ID:      whereHelperuint{field: "`cmf_balance_charge_admin`.`id`"},
	Touid:   whereHelperint{field: "`cmf_balance_charge_admin`.`touid`"},
	Balance: whereHelpertypes_Decimal{field: "`cmf_balance_charge_admin`.`balance`"},
	Addtime: whereHelperint{field: "`cmf_balance_charge_admin`.`addtime`"},
	Admin:   whereHelperstring{field: "`cmf_balance_charge_admin`.`admin`"},
	IP:      whereHelperstring{field: "`cmf_balance_charge_admin`.`ip`"},
}

// CMFBalanceChargeAdminRels is where relationship names are stored.
var CMFBalanceChargeAdminRels = struct {
}{}

// cmfBalanceChargeAdminR is where relationships are stored.
type cmfBalanceChargeAdminR struct {
}

// NewStruct creates a new relationship struct
func (*cmfBalanceChargeAdminR) NewStruct() *cmfBalanceChargeAdminR {
	return &cmfBalanceChargeAdminR{}
}

// cmfBalanceChargeAdminL is where Load methods for each relationship are stored.
type cmfBalanceChargeAdminL struct{}

var (
	cmfBalanceChargeAdminAllColumns            = []string{"id", "touid", "balance", "addtime", "admin", "ip"}
	cmfBalanceChargeAdminColumnsWithoutDefault = []string{"admin", "ip"}
	cmfBalanceChargeAdminColumnsWithDefault    = []string{"id", "touid", "balance", "addtime"}
	cmfBalanceChargeAdminPrimaryKeyColumns     = []string{"id"}
)

type (
	// CMFBalanceChargeAdminSlice is an alias for a slice of pointers to CMFBalanceChargeAdmin.
	// This should generally be used opposed to []CMFBalanceChargeAdmin.
	CMFBalanceChargeAdminSlice []*CMFBalanceChargeAdmin
	// CMFBalanceChargeAdminHook is the signature for custom CMFBalanceChargeAdmin hook methods
	CMFBalanceChargeAdminHook func(context.Context, boil.ContextExecutor, *CMFBalanceChargeAdmin) error

	cmfBalanceChargeAdminQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	cmfBalanceChargeAdminType                 = reflect.TypeOf(&CMFBalanceChargeAdmin{})
	cmfBalanceChargeAdminMapping              = queries.MakeStructMapping(cmfBalanceChargeAdminType)
	cmfBalanceChargeAdminPrimaryKeyMapping, _ = queries.BindMapping(cmfBalanceChargeAdminType, cmfBalanceChargeAdminMapping, cmfBalanceChargeAdminPrimaryKeyColumns)
	cmfBalanceChargeAdminInsertCacheMut       sync.RWMutex
	cmfBalanceChargeAdminInsertCache          = make(map[string]insertCache)
	cmfBalanceChargeAdminUpdateCacheMut       sync.RWMutex
	cmfBalanceChargeAdminUpdateCache          = make(map[string]updateCache)
	cmfBalanceChargeAdminUpsertCacheMut       sync.RWMutex
	cmfBalanceChargeAdminUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var cmfBalanceChargeAdminBeforeInsertHooks []CMFBalanceChargeAdminHook
var cmfBalanceChargeAdminBeforeUpdateHooks []CMFBalanceChargeAdminHook
var cmfBalanceChargeAdminBeforeDeleteHooks []CMFBalanceChargeAdminHook
var cmfBalanceChargeAdminBeforeUpsertHooks []CMFBalanceChargeAdminHook

var cmfBalanceChargeAdminAfterInsertHooks []CMFBalanceChargeAdminHook
var cmfBalanceChargeAdminAfterSelectHooks []CMFBalanceChargeAdminHook
var cmfBalanceChargeAdminAfterUpdateHooks []CMFBalanceChargeAdminHook
var cmfBalanceChargeAdminAfterDeleteHooks []CMFBalanceChargeAdminHook
var cmfBalanceChargeAdminAfterUpsertHooks []CMFBalanceChargeAdminHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CMFBalanceChargeAdmin) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfBalanceChargeAdminBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CMFBalanceChargeAdmin) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfBalanceChargeAdminBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CMFBalanceChargeAdmin) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfBalanceChargeAdminBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CMFBalanceChargeAdmin) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfBalanceChargeAdminBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CMFBalanceChargeAdmin) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfBalanceChargeAdminAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CMFBalanceChargeAdmin) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfBalanceChargeAdminAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CMFBalanceChargeAdmin) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfBalanceChargeAdminAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CMFBalanceChargeAdmin) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfBalanceChargeAdminAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CMFBalanceChargeAdmin) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfBalanceChargeAdminAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCMFBalanceChargeAdminHook registers your hook function for all future operations.
func AddCMFBalanceChargeAdminHook(hookPoint boil.HookPoint, cmfBalanceChargeAdminHook CMFBalanceChargeAdminHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		cmfBalanceChargeAdminBeforeInsertHooks = append(cmfBalanceChargeAdminBeforeInsertHooks, cmfBalanceChargeAdminHook)
	case boil.BeforeUpdateHook:
		cmfBalanceChargeAdminBeforeUpdateHooks = append(cmfBalanceChargeAdminBeforeUpdateHooks, cmfBalanceChargeAdminHook)
	case boil.BeforeDeleteHook:
		cmfBalanceChargeAdminBeforeDeleteHooks = append(cmfBalanceChargeAdminBeforeDeleteHooks, cmfBalanceChargeAdminHook)
	case boil.BeforeUpsertHook:
		cmfBalanceChargeAdminBeforeUpsertHooks = append(cmfBalanceChargeAdminBeforeUpsertHooks, cmfBalanceChargeAdminHook)
	case boil.AfterInsertHook:
		cmfBalanceChargeAdminAfterInsertHooks = append(cmfBalanceChargeAdminAfterInsertHooks, cmfBalanceChargeAdminHook)
	case boil.AfterSelectHook:
		cmfBalanceChargeAdminAfterSelectHooks = append(cmfBalanceChargeAdminAfterSelectHooks, cmfBalanceChargeAdminHook)
	case boil.AfterUpdateHook:
		cmfBalanceChargeAdminAfterUpdateHooks = append(cmfBalanceChargeAdminAfterUpdateHooks, cmfBalanceChargeAdminHook)
	case boil.AfterDeleteHook:
		cmfBalanceChargeAdminAfterDeleteHooks = append(cmfBalanceChargeAdminAfterDeleteHooks, cmfBalanceChargeAdminHook)
	case boil.AfterUpsertHook:
		cmfBalanceChargeAdminAfterUpsertHooks = append(cmfBalanceChargeAdminAfterUpsertHooks, cmfBalanceChargeAdminHook)
	}
}

// One returns a single cmfBalanceChargeAdmin record from the query.
func (q cmfBalanceChargeAdminQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CMFBalanceChargeAdmin, error) {
	o := &CMFBalanceChargeAdmin{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for cmf_balance_charge_admin")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all CMFBalanceChargeAdmin records from the query.
func (q cmfBalanceChargeAdminQuery) All(ctx context.Context, exec boil.ContextExecutor) (CMFBalanceChargeAdminSlice, error) {
	var o []*CMFBalanceChargeAdmin

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CMFBalanceChargeAdmin slice")
	}

	if len(cmfBalanceChargeAdminAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all CMFBalanceChargeAdmin records in the query.
func (q cmfBalanceChargeAdminQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count cmf_balance_charge_admin rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q cmfBalanceChargeAdminQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if cmf_balance_charge_admin exists")
	}

	return count > 0, nil
}

// CMFBalanceChargeAdmins retrieves all the records using an executor.
func CMFBalanceChargeAdmins(mods ...qm.QueryMod) cmfBalanceChargeAdminQuery {
	mods = append(mods, qm.From("`cmf_balance_charge_admin`"))
	return cmfBalanceChargeAdminQuery{NewQuery(mods...)}
}

// FindCMFBalanceChargeAdmin retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCMFBalanceChargeAdmin(ctx context.Context, exec boil.ContextExecutor, iD uint, selectCols ...string) (*CMFBalanceChargeAdmin, error) {
	cmfBalanceChargeAdminObj := &CMFBalanceChargeAdmin{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `cmf_balance_charge_admin` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, cmfBalanceChargeAdminObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from cmf_balance_charge_admin")
	}

	return cmfBalanceChargeAdminObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CMFBalanceChargeAdmin) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cmf_balance_charge_admin provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(cmfBalanceChargeAdminColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	cmfBalanceChargeAdminInsertCacheMut.RLock()
	cache, cached := cmfBalanceChargeAdminInsertCache[key]
	cmfBalanceChargeAdminInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			cmfBalanceChargeAdminAllColumns,
			cmfBalanceChargeAdminColumnsWithDefault,
			cmfBalanceChargeAdminColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(cmfBalanceChargeAdminType, cmfBalanceChargeAdminMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(cmfBalanceChargeAdminType, cmfBalanceChargeAdminMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `cmf_balance_charge_admin` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `cmf_balance_charge_admin` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `cmf_balance_charge_admin` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, cmfBalanceChargeAdminPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into cmf_balance_charge_admin")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == cmfBalanceChargeAdminMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cmf_balance_charge_admin")
	}

CacheNoHooks:
	if !cached {
		cmfBalanceChargeAdminInsertCacheMut.Lock()
		cmfBalanceChargeAdminInsertCache[key] = cache
		cmfBalanceChargeAdminInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the CMFBalanceChargeAdmin.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CMFBalanceChargeAdmin) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	cmfBalanceChargeAdminUpdateCacheMut.RLock()
	cache, cached := cmfBalanceChargeAdminUpdateCache[key]
	cmfBalanceChargeAdminUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			cmfBalanceChargeAdminAllColumns,
			cmfBalanceChargeAdminPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update cmf_balance_charge_admin, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `cmf_balance_charge_admin` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, cmfBalanceChargeAdminPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(cmfBalanceChargeAdminType, cmfBalanceChargeAdminMapping, append(wl, cmfBalanceChargeAdminPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update cmf_balance_charge_admin row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for cmf_balance_charge_admin")
	}

	if !cached {
		cmfBalanceChargeAdminUpdateCacheMut.Lock()
		cmfBalanceChargeAdminUpdateCache[key] = cache
		cmfBalanceChargeAdminUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q cmfBalanceChargeAdminQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for cmf_balance_charge_admin")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for cmf_balance_charge_admin")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CMFBalanceChargeAdminSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfBalanceChargeAdminPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `cmf_balance_charge_admin` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfBalanceChargeAdminPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in cmfBalanceChargeAdmin slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all cmfBalanceChargeAdmin")
	}
	return rowsAff, nil
}

var mySQLCMFBalanceChargeAdminUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CMFBalanceChargeAdmin) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cmf_balance_charge_admin provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(cmfBalanceChargeAdminColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLCMFBalanceChargeAdminUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	cmfBalanceChargeAdminUpsertCacheMut.RLock()
	cache, cached := cmfBalanceChargeAdminUpsertCache[key]
	cmfBalanceChargeAdminUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			cmfBalanceChargeAdminAllColumns,
			cmfBalanceChargeAdminColumnsWithDefault,
			cmfBalanceChargeAdminColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			cmfBalanceChargeAdminAllColumns,
			cmfBalanceChargeAdminPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert cmf_balance_charge_admin, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`cmf_balance_charge_admin`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `cmf_balance_charge_admin` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(cmfBalanceChargeAdminType, cmfBalanceChargeAdminMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(cmfBalanceChargeAdminType, cmfBalanceChargeAdminMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for cmf_balance_charge_admin")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == cmfBalanceChargeAdminMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(cmfBalanceChargeAdminType, cmfBalanceChargeAdminMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for cmf_balance_charge_admin")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cmf_balance_charge_admin")
	}

CacheNoHooks:
	if !cached {
		cmfBalanceChargeAdminUpsertCacheMut.Lock()
		cmfBalanceChargeAdminUpsertCache[key] = cache
		cmfBalanceChargeAdminUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single CMFBalanceChargeAdmin record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CMFBalanceChargeAdmin) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CMFBalanceChargeAdmin provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cmfBalanceChargeAdminPrimaryKeyMapping)
	sql := "DELETE FROM `cmf_balance_charge_admin` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from cmf_balance_charge_admin")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for cmf_balance_charge_admin")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q cmfBalanceChargeAdminQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no cmfBalanceChargeAdminQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cmf_balance_charge_admin")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cmf_balance_charge_admin")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CMFBalanceChargeAdminSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(cmfBalanceChargeAdminBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfBalanceChargeAdminPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `cmf_balance_charge_admin` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfBalanceChargeAdminPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cmfBalanceChargeAdmin slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cmf_balance_charge_admin")
	}

	if len(cmfBalanceChargeAdminAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CMFBalanceChargeAdmin) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCMFBalanceChargeAdmin(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CMFBalanceChargeAdminSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CMFBalanceChargeAdminSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfBalanceChargeAdminPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `cmf_balance_charge_admin`.* FROM `cmf_balance_charge_admin` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfBalanceChargeAdminPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CMFBalanceChargeAdminSlice")
	}

	*o = slice

	return nil
}

// CMFBalanceChargeAdminExists checks if the CMFBalanceChargeAdmin row exists.
func CMFBalanceChargeAdminExists(ctx context.Context, exec boil.ContextExecutor, iD uint) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `cmf_balance_charge_admin` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if cmf_balance_charge_admin exists")
	}

	return exists, nil
}
