// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// CMFChargeAdmin is an object representing the database table.
type CMFChargeAdmin struct {
	ID      uint   `boil:"id" json:"id" toml:"id" yaml:"id"`
	Touid   int    `boil:"touid" json:"touid" toml:"touid" yaml:"touid"`
	Coin    int    `boil:"coin" json:"coin" toml:"coin" yaml:"coin"`
	Addtime int    `boil:"addtime" json:"addtime" toml:"addtime" yaml:"addtime"`
	Admin   string `boil:"admin" json:"admin" toml:"admin" yaml:"admin"`
	IP      string `boil:"ip" json:"ip" toml:"ip" yaml:"ip"`

	R *cmfChargeAdminR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L cmfChargeAdminL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CMFChargeAdminColumns = struct {
	ID      string
	Touid   string
	Coin    string
	Addtime string
	Admin   string
	IP      string
}{
	ID:      "id",
	Touid:   "touid",
	Coin:    "coin",
	Addtime: "addtime",
	Admin:   "admin",
	IP:      "ip",
}

// Generated where

var CMFChargeAdminWhere = struct {
	ID      whereHelperuint
	Touid   whereHelperint
	Coin    whereHelperint
	Addtime whereHelperint
	Admin   whereHelperstring
	IP      whereHelperstring
}{
	ID:      whereHelperuint{field: "`cmf_charge_admin`.`id`"},
	Touid:   whereHelperint{field: "`cmf_charge_admin`.`touid`"},
	Coin:    whereHelperint{field: "`cmf_charge_admin`.`coin`"},
	Addtime: whereHelperint{field: "`cmf_charge_admin`.`addtime`"},
	Admin:   whereHelperstring{field: "`cmf_charge_admin`.`admin`"},
	IP:      whereHelperstring{field: "`cmf_charge_admin`.`ip`"},
}

// CMFChargeAdminRels is where relationship names are stored.
var CMFChargeAdminRels = struct {
}{}

// cmfChargeAdminR is where relationships are stored.
type cmfChargeAdminR struct {
}

// NewStruct creates a new relationship struct
func (*cmfChargeAdminR) NewStruct() *cmfChargeAdminR {
	return &cmfChargeAdminR{}
}

// cmfChargeAdminL is where Load methods for each relationship are stored.
type cmfChargeAdminL struct{}

var (
	cmfChargeAdminAllColumns            = []string{"id", "touid", "coin", "addtime", "admin", "ip"}
	cmfChargeAdminColumnsWithoutDefault = []string{"admin", "ip"}
	cmfChargeAdminColumnsWithDefault    = []string{"id", "touid", "coin", "addtime"}
	cmfChargeAdminPrimaryKeyColumns     = []string{"id"}
)

type (
	// CMFChargeAdminSlice is an alias for a slice of pointers to CMFChargeAdmin.
	// This should generally be used opposed to []CMFChargeAdmin.
	CMFChargeAdminSlice []*CMFChargeAdmin
	// CMFChargeAdminHook is the signature for custom CMFChargeAdmin hook methods
	CMFChargeAdminHook func(context.Context, boil.ContextExecutor, *CMFChargeAdmin) error

	cmfChargeAdminQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	cmfChargeAdminType                 = reflect.TypeOf(&CMFChargeAdmin{})
	cmfChargeAdminMapping              = queries.MakeStructMapping(cmfChargeAdminType)
	cmfChargeAdminPrimaryKeyMapping, _ = queries.BindMapping(cmfChargeAdminType, cmfChargeAdminMapping, cmfChargeAdminPrimaryKeyColumns)
	cmfChargeAdminInsertCacheMut       sync.RWMutex
	cmfChargeAdminInsertCache          = make(map[string]insertCache)
	cmfChargeAdminUpdateCacheMut       sync.RWMutex
	cmfChargeAdminUpdateCache          = make(map[string]updateCache)
	cmfChargeAdminUpsertCacheMut       sync.RWMutex
	cmfChargeAdminUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var cmfChargeAdminBeforeInsertHooks []CMFChargeAdminHook
var cmfChargeAdminBeforeUpdateHooks []CMFChargeAdminHook
var cmfChargeAdminBeforeDeleteHooks []CMFChargeAdminHook
var cmfChargeAdminBeforeUpsertHooks []CMFChargeAdminHook

var cmfChargeAdminAfterInsertHooks []CMFChargeAdminHook
var cmfChargeAdminAfterSelectHooks []CMFChargeAdminHook
var cmfChargeAdminAfterUpdateHooks []CMFChargeAdminHook
var cmfChargeAdminAfterDeleteHooks []CMFChargeAdminHook
var cmfChargeAdminAfterUpsertHooks []CMFChargeAdminHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CMFChargeAdmin) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfChargeAdminBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CMFChargeAdmin) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfChargeAdminBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CMFChargeAdmin) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfChargeAdminBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CMFChargeAdmin) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfChargeAdminBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CMFChargeAdmin) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfChargeAdminAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CMFChargeAdmin) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfChargeAdminAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CMFChargeAdmin) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfChargeAdminAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CMFChargeAdmin) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfChargeAdminAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CMFChargeAdmin) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfChargeAdminAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCMFChargeAdminHook registers your hook function for all future operations.
func AddCMFChargeAdminHook(hookPoint boil.HookPoint, cmfChargeAdminHook CMFChargeAdminHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		cmfChargeAdminBeforeInsertHooks = append(cmfChargeAdminBeforeInsertHooks, cmfChargeAdminHook)
	case boil.BeforeUpdateHook:
		cmfChargeAdminBeforeUpdateHooks = append(cmfChargeAdminBeforeUpdateHooks, cmfChargeAdminHook)
	case boil.BeforeDeleteHook:
		cmfChargeAdminBeforeDeleteHooks = append(cmfChargeAdminBeforeDeleteHooks, cmfChargeAdminHook)
	case boil.BeforeUpsertHook:
		cmfChargeAdminBeforeUpsertHooks = append(cmfChargeAdminBeforeUpsertHooks, cmfChargeAdminHook)
	case boil.AfterInsertHook:
		cmfChargeAdminAfterInsertHooks = append(cmfChargeAdminAfterInsertHooks, cmfChargeAdminHook)
	case boil.AfterSelectHook:
		cmfChargeAdminAfterSelectHooks = append(cmfChargeAdminAfterSelectHooks, cmfChargeAdminHook)
	case boil.AfterUpdateHook:
		cmfChargeAdminAfterUpdateHooks = append(cmfChargeAdminAfterUpdateHooks, cmfChargeAdminHook)
	case boil.AfterDeleteHook:
		cmfChargeAdminAfterDeleteHooks = append(cmfChargeAdminAfterDeleteHooks, cmfChargeAdminHook)
	case boil.AfterUpsertHook:
		cmfChargeAdminAfterUpsertHooks = append(cmfChargeAdminAfterUpsertHooks, cmfChargeAdminHook)
	}
}

// One returns a single cmfChargeAdmin record from the query.
func (q cmfChargeAdminQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CMFChargeAdmin, error) {
	o := &CMFChargeAdmin{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for cmf_charge_admin")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all CMFChargeAdmin records from the query.
func (q cmfChargeAdminQuery) All(ctx context.Context, exec boil.ContextExecutor) (CMFChargeAdminSlice, error) {
	var o []*CMFChargeAdmin

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CMFChargeAdmin slice")
	}

	if len(cmfChargeAdminAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all CMFChargeAdmin records in the query.
func (q cmfChargeAdminQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count cmf_charge_admin rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q cmfChargeAdminQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if cmf_charge_admin exists")
	}

	return count > 0, nil
}

// CMFChargeAdmins retrieves all the records using an executor.
func CMFChargeAdmins(mods ...qm.QueryMod) cmfChargeAdminQuery {
	mods = append(mods, qm.From("`cmf_charge_admin`"))
	return cmfChargeAdminQuery{NewQuery(mods...)}
}

// FindCMFChargeAdmin retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCMFChargeAdmin(ctx context.Context, exec boil.ContextExecutor, iD uint, selectCols ...string) (*CMFChargeAdmin, error) {
	cmfChargeAdminObj := &CMFChargeAdmin{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `cmf_charge_admin` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, cmfChargeAdminObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from cmf_charge_admin")
	}

	return cmfChargeAdminObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CMFChargeAdmin) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cmf_charge_admin provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(cmfChargeAdminColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	cmfChargeAdminInsertCacheMut.RLock()
	cache, cached := cmfChargeAdminInsertCache[key]
	cmfChargeAdminInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			cmfChargeAdminAllColumns,
			cmfChargeAdminColumnsWithDefault,
			cmfChargeAdminColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(cmfChargeAdminType, cmfChargeAdminMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(cmfChargeAdminType, cmfChargeAdminMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `cmf_charge_admin` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `cmf_charge_admin` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `cmf_charge_admin` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, cmfChargeAdminPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into cmf_charge_admin")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == cmfChargeAdminMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cmf_charge_admin")
	}

CacheNoHooks:
	if !cached {
		cmfChargeAdminInsertCacheMut.Lock()
		cmfChargeAdminInsertCache[key] = cache
		cmfChargeAdminInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the CMFChargeAdmin.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CMFChargeAdmin) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	cmfChargeAdminUpdateCacheMut.RLock()
	cache, cached := cmfChargeAdminUpdateCache[key]
	cmfChargeAdminUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			cmfChargeAdminAllColumns,
			cmfChargeAdminPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update cmf_charge_admin, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `cmf_charge_admin` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, cmfChargeAdminPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(cmfChargeAdminType, cmfChargeAdminMapping, append(wl, cmfChargeAdminPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update cmf_charge_admin row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for cmf_charge_admin")
	}

	if !cached {
		cmfChargeAdminUpdateCacheMut.Lock()
		cmfChargeAdminUpdateCache[key] = cache
		cmfChargeAdminUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q cmfChargeAdminQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for cmf_charge_admin")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for cmf_charge_admin")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CMFChargeAdminSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfChargeAdminPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `cmf_charge_admin` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfChargeAdminPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in cmfChargeAdmin slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all cmfChargeAdmin")
	}
	return rowsAff, nil
}

var mySQLCMFChargeAdminUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CMFChargeAdmin) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cmf_charge_admin provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(cmfChargeAdminColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLCMFChargeAdminUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	cmfChargeAdminUpsertCacheMut.RLock()
	cache, cached := cmfChargeAdminUpsertCache[key]
	cmfChargeAdminUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			cmfChargeAdminAllColumns,
			cmfChargeAdminColumnsWithDefault,
			cmfChargeAdminColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			cmfChargeAdminAllColumns,
			cmfChargeAdminPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert cmf_charge_admin, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`cmf_charge_admin`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `cmf_charge_admin` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(cmfChargeAdminType, cmfChargeAdminMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(cmfChargeAdminType, cmfChargeAdminMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for cmf_charge_admin")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == cmfChargeAdminMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(cmfChargeAdminType, cmfChargeAdminMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for cmf_charge_admin")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cmf_charge_admin")
	}

CacheNoHooks:
	if !cached {
		cmfChargeAdminUpsertCacheMut.Lock()
		cmfChargeAdminUpsertCache[key] = cache
		cmfChargeAdminUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single CMFChargeAdmin record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CMFChargeAdmin) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CMFChargeAdmin provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cmfChargeAdminPrimaryKeyMapping)
	sql := "DELETE FROM `cmf_charge_admin` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from cmf_charge_admin")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for cmf_charge_admin")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q cmfChargeAdminQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no cmfChargeAdminQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cmf_charge_admin")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cmf_charge_admin")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CMFChargeAdminSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(cmfChargeAdminBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfChargeAdminPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `cmf_charge_admin` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfChargeAdminPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cmfChargeAdmin slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cmf_charge_admin")
	}

	if len(cmfChargeAdminAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CMFChargeAdmin) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCMFChargeAdmin(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CMFChargeAdminSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CMFChargeAdminSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfChargeAdminPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `cmf_charge_admin`.* FROM `cmf_charge_admin` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfChargeAdminPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CMFChargeAdminSlice")
	}

	*o = slice

	return nil
}

// CMFChargeAdminExists checks if the CMFChargeAdmin row exists.
func CMFChargeAdminExists(ctx context.Context, exec boil.ContextExecutor, iD uint) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `cmf_charge_admin` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if cmf_charge_admin exists")
	}

	return exists, nil
}
