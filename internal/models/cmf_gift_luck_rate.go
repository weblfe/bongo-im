// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// CMFGiftLuckRate is an object representing the database table.
type CMFGiftLuckRate struct {
	ID     uint64        `boil:"id" json:"id" toml:"id" yaml:"id"`
	Giftid int           `boil:"giftid" json:"giftid" toml:"giftid" yaml:"giftid"`
	Nums   uint          `boil:"nums" json:"nums" toml:"nums" yaml:"nums"`
	Times  uint          `boil:"times" json:"times" toml:"times" yaml:"times"`
	Rate   types.Decimal `boil:"rate" json:"rate" toml:"rate" yaml:"rate"`
	Isall  int8          `boil:"isall" json:"isall" toml:"isall" yaml:"isall"`

	R *cmfGiftLuckRateR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L cmfGiftLuckRateL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CMFGiftLuckRateColumns = struct {
	ID     string
	Giftid string
	Nums   string
	Times  string
	Rate   string
	Isall  string
}{
	ID:     "id",
	Giftid: "giftid",
	Nums:   "nums",
	Times:  "times",
	Rate:   "rate",
	Isall:  "isall",
}

// Generated where

var CMFGiftLuckRateWhere = struct {
	ID     whereHelperuint64
	Giftid whereHelperint
	Nums   whereHelperuint
	Times  whereHelperuint
	Rate   whereHelpertypes_Decimal
	Isall  whereHelperint8
}{
	ID:     whereHelperuint64{field: "`cmf_gift_luck_rate`.`id`"},
	Giftid: whereHelperint{field: "`cmf_gift_luck_rate`.`giftid`"},
	Nums:   whereHelperuint{field: "`cmf_gift_luck_rate`.`nums`"},
	Times:  whereHelperuint{field: "`cmf_gift_luck_rate`.`times`"},
	Rate:   whereHelpertypes_Decimal{field: "`cmf_gift_luck_rate`.`rate`"},
	Isall:  whereHelperint8{field: "`cmf_gift_luck_rate`.`isall`"},
}

// CMFGiftLuckRateRels is where relationship names are stored.
var CMFGiftLuckRateRels = struct {
}{}

// cmfGiftLuckRateR is where relationships are stored.
type cmfGiftLuckRateR struct {
}

// NewStruct creates a new relationship struct
func (*cmfGiftLuckRateR) NewStruct() *cmfGiftLuckRateR {
	return &cmfGiftLuckRateR{}
}

// cmfGiftLuckRateL is where Load methods for each relationship are stored.
type cmfGiftLuckRateL struct{}

var (
	cmfGiftLuckRateAllColumns            = []string{"id", "giftid", "nums", "times", "rate", "isall"}
	cmfGiftLuckRateColumnsWithoutDefault = []string{"nums"}
	cmfGiftLuckRateColumnsWithDefault    = []string{"id", "giftid", "times", "rate", "isall"}
	cmfGiftLuckRatePrimaryKeyColumns     = []string{"id"}
)

type (
	// CMFGiftLuckRateSlice is an alias for a slice of pointers to CMFGiftLuckRate.
	// This should generally be used opposed to []CMFGiftLuckRate.
	CMFGiftLuckRateSlice []*CMFGiftLuckRate
	// CMFGiftLuckRateHook is the signature for custom CMFGiftLuckRate hook methods
	CMFGiftLuckRateHook func(context.Context, boil.ContextExecutor, *CMFGiftLuckRate) error

	cmfGiftLuckRateQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	cmfGiftLuckRateType                 = reflect.TypeOf(&CMFGiftLuckRate{})
	cmfGiftLuckRateMapping              = queries.MakeStructMapping(cmfGiftLuckRateType)
	cmfGiftLuckRatePrimaryKeyMapping, _ = queries.BindMapping(cmfGiftLuckRateType, cmfGiftLuckRateMapping, cmfGiftLuckRatePrimaryKeyColumns)
	cmfGiftLuckRateInsertCacheMut       sync.RWMutex
	cmfGiftLuckRateInsertCache          = make(map[string]insertCache)
	cmfGiftLuckRateUpdateCacheMut       sync.RWMutex
	cmfGiftLuckRateUpdateCache          = make(map[string]updateCache)
	cmfGiftLuckRateUpsertCacheMut       sync.RWMutex
	cmfGiftLuckRateUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var cmfGiftLuckRateBeforeInsertHooks []CMFGiftLuckRateHook
var cmfGiftLuckRateBeforeUpdateHooks []CMFGiftLuckRateHook
var cmfGiftLuckRateBeforeDeleteHooks []CMFGiftLuckRateHook
var cmfGiftLuckRateBeforeUpsertHooks []CMFGiftLuckRateHook

var cmfGiftLuckRateAfterInsertHooks []CMFGiftLuckRateHook
var cmfGiftLuckRateAfterSelectHooks []CMFGiftLuckRateHook
var cmfGiftLuckRateAfterUpdateHooks []CMFGiftLuckRateHook
var cmfGiftLuckRateAfterDeleteHooks []CMFGiftLuckRateHook
var cmfGiftLuckRateAfterUpsertHooks []CMFGiftLuckRateHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CMFGiftLuckRate) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfGiftLuckRateBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CMFGiftLuckRate) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfGiftLuckRateBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CMFGiftLuckRate) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfGiftLuckRateBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CMFGiftLuckRate) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfGiftLuckRateBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CMFGiftLuckRate) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfGiftLuckRateAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CMFGiftLuckRate) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfGiftLuckRateAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CMFGiftLuckRate) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfGiftLuckRateAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CMFGiftLuckRate) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfGiftLuckRateAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CMFGiftLuckRate) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfGiftLuckRateAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCMFGiftLuckRateHook registers your hook function for all future operations.
func AddCMFGiftLuckRateHook(hookPoint boil.HookPoint, cmfGiftLuckRateHook CMFGiftLuckRateHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		cmfGiftLuckRateBeforeInsertHooks = append(cmfGiftLuckRateBeforeInsertHooks, cmfGiftLuckRateHook)
	case boil.BeforeUpdateHook:
		cmfGiftLuckRateBeforeUpdateHooks = append(cmfGiftLuckRateBeforeUpdateHooks, cmfGiftLuckRateHook)
	case boil.BeforeDeleteHook:
		cmfGiftLuckRateBeforeDeleteHooks = append(cmfGiftLuckRateBeforeDeleteHooks, cmfGiftLuckRateHook)
	case boil.BeforeUpsertHook:
		cmfGiftLuckRateBeforeUpsertHooks = append(cmfGiftLuckRateBeforeUpsertHooks, cmfGiftLuckRateHook)
	case boil.AfterInsertHook:
		cmfGiftLuckRateAfterInsertHooks = append(cmfGiftLuckRateAfterInsertHooks, cmfGiftLuckRateHook)
	case boil.AfterSelectHook:
		cmfGiftLuckRateAfterSelectHooks = append(cmfGiftLuckRateAfterSelectHooks, cmfGiftLuckRateHook)
	case boil.AfterUpdateHook:
		cmfGiftLuckRateAfterUpdateHooks = append(cmfGiftLuckRateAfterUpdateHooks, cmfGiftLuckRateHook)
	case boil.AfterDeleteHook:
		cmfGiftLuckRateAfterDeleteHooks = append(cmfGiftLuckRateAfterDeleteHooks, cmfGiftLuckRateHook)
	case boil.AfterUpsertHook:
		cmfGiftLuckRateAfterUpsertHooks = append(cmfGiftLuckRateAfterUpsertHooks, cmfGiftLuckRateHook)
	}
}

// One returns a single cmfGiftLuckRate record from the query.
func (q cmfGiftLuckRateQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CMFGiftLuckRate, error) {
	o := &CMFGiftLuckRate{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for cmf_gift_luck_rate")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all CMFGiftLuckRate records from the query.
func (q cmfGiftLuckRateQuery) All(ctx context.Context, exec boil.ContextExecutor) (CMFGiftLuckRateSlice, error) {
	var o []*CMFGiftLuckRate

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CMFGiftLuckRate slice")
	}

	if len(cmfGiftLuckRateAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all CMFGiftLuckRate records in the query.
func (q cmfGiftLuckRateQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count cmf_gift_luck_rate rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q cmfGiftLuckRateQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if cmf_gift_luck_rate exists")
	}

	return count > 0, nil
}

// CMFGiftLuckRates retrieves all the records using an executor.
func CMFGiftLuckRates(mods ...qm.QueryMod) cmfGiftLuckRateQuery {
	mods = append(mods, qm.From("`cmf_gift_luck_rate`"))
	return cmfGiftLuckRateQuery{NewQuery(mods...)}
}

// FindCMFGiftLuckRate retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCMFGiftLuckRate(ctx context.Context, exec boil.ContextExecutor, iD uint64, selectCols ...string) (*CMFGiftLuckRate, error) {
	cmfGiftLuckRateObj := &CMFGiftLuckRate{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `cmf_gift_luck_rate` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, cmfGiftLuckRateObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from cmf_gift_luck_rate")
	}

	return cmfGiftLuckRateObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CMFGiftLuckRate) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cmf_gift_luck_rate provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(cmfGiftLuckRateColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	cmfGiftLuckRateInsertCacheMut.RLock()
	cache, cached := cmfGiftLuckRateInsertCache[key]
	cmfGiftLuckRateInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			cmfGiftLuckRateAllColumns,
			cmfGiftLuckRateColumnsWithDefault,
			cmfGiftLuckRateColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(cmfGiftLuckRateType, cmfGiftLuckRateMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(cmfGiftLuckRateType, cmfGiftLuckRateMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `cmf_gift_luck_rate` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `cmf_gift_luck_rate` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `cmf_gift_luck_rate` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, cmfGiftLuckRatePrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into cmf_gift_luck_rate")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == cmfGiftLuckRateMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cmf_gift_luck_rate")
	}

CacheNoHooks:
	if !cached {
		cmfGiftLuckRateInsertCacheMut.Lock()
		cmfGiftLuckRateInsertCache[key] = cache
		cmfGiftLuckRateInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the CMFGiftLuckRate.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CMFGiftLuckRate) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	cmfGiftLuckRateUpdateCacheMut.RLock()
	cache, cached := cmfGiftLuckRateUpdateCache[key]
	cmfGiftLuckRateUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			cmfGiftLuckRateAllColumns,
			cmfGiftLuckRatePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update cmf_gift_luck_rate, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `cmf_gift_luck_rate` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, cmfGiftLuckRatePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(cmfGiftLuckRateType, cmfGiftLuckRateMapping, append(wl, cmfGiftLuckRatePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update cmf_gift_luck_rate row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for cmf_gift_luck_rate")
	}

	if !cached {
		cmfGiftLuckRateUpdateCacheMut.Lock()
		cmfGiftLuckRateUpdateCache[key] = cache
		cmfGiftLuckRateUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q cmfGiftLuckRateQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for cmf_gift_luck_rate")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for cmf_gift_luck_rate")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CMFGiftLuckRateSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfGiftLuckRatePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `cmf_gift_luck_rate` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfGiftLuckRatePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in cmfGiftLuckRate slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all cmfGiftLuckRate")
	}
	return rowsAff, nil
}

var mySQLCMFGiftLuckRateUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CMFGiftLuckRate) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cmf_gift_luck_rate provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(cmfGiftLuckRateColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLCMFGiftLuckRateUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	cmfGiftLuckRateUpsertCacheMut.RLock()
	cache, cached := cmfGiftLuckRateUpsertCache[key]
	cmfGiftLuckRateUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			cmfGiftLuckRateAllColumns,
			cmfGiftLuckRateColumnsWithDefault,
			cmfGiftLuckRateColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			cmfGiftLuckRateAllColumns,
			cmfGiftLuckRatePrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert cmf_gift_luck_rate, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`cmf_gift_luck_rate`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `cmf_gift_luck_rate` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(cmfGiftLuckRateType, cmfGiftLuckRateMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(cmfGiftLuckRateType, cmfGiftLuckRateMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for cmf_gift_luck_rate")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == cmfGiftLuckRateMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(cmfGiftLuckRateType, cmfGiftLuckRateMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for cmf_gift_luck_rate")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cmf_gift_luck_rate")
	}

CacheNoHooks:
	if !cached {
		cmfGiftLuckRateUpsertCacheMut.Lock()
		cmfGiftLuckRateUpsertCache[key] = cache
		cmfGiftLuckRateUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single CMFGiftLuckRate record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CMFGiftLuckRate) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CMFGiftLuckRate provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cmfGiftLuckRatePrimaryKeyMapping)
	sql := "DELETE FROM `cmf_gift_luck_rate` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from cmf_gift_luck_rate")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for cmf_gift_luck_rate")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q cmfGiftLuckRateQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no cmfGiftLuckRateQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cmf_gift_luck_rate")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cmf_gift_luck_rate")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CMFGiftLuckRateSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(cmfGiftLuckRateBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfGiftLuckRatePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `cmf_gift_luck_rate` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfGiftLuckRatePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cmfGiftLuckRate slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cmf_gift_luck_rate")
	}

	if len(cmfGiftLuckRateAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CMFGiftLuckRate) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCMFGiftLuckRate(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CMFGiftLuckRateSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CMFGiftLuckRateSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfGiftLuckRatePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `cmf_gift_luck_rate`.* FROM `cmf_gift_luck_rate` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfGiftLuckRatePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CMFGiftLuckRateSlice")
	}

	*o = slice

	return nil
}

// CMFGiftLuckRateExists checks if the CMFGiftLuckRate row exists.
func CMFGiftLuckRateExists(ctx context.Context, exec boil.ContextExecutor, iD uint64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `cmf_gift_luck_rate` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if cmf_gift_luck_rate exists")
	}

	return exists, nil
}
