// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// CMFGuard is an object representing the database table.
type CMFGuard struct {
	ID         uint   `boil:"id" json:"id" toml:"id" yaml:"id"`
	Name       string `boil:"name" json:"name" toml:"name" yaml:"name"`
	Type       bool   `boil:"type" json:"type" toml:"type" yaml:"type"`
	Coin       int    `boil:"coin" json:"coin" toml:"coin" yaml:"coin"`
	LengthType bool   `boil:"length_type" json:"length_type" toml:"length_type" yaml:"length_type"`
	Length     int    `boil:"length" json:"length" toml:"length" yaml:"length"`
	LengthTime int    `boil:"length_time" json:"length_time" toml:"length_time" yaml:"length_time"`
	Addtime    int    `boil:"addtime" json:"addtime" toml:"addtime" yaml:"addtime"`
	Uptime     int    `boil:"uptime" json:"uptime" toml:"uptime" yaml:"uptime"`
	ListOrder  int    `boil:"list_order" json:"list_order" toml:"list_order" yaml:"list_order"`

	R *cmfGuardR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L cmfGuardL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CMFGuardColumns = struct {
	ID         string
	Name       string
	Type       string
	Coin       string
	LengthType string
	Length     string
	LengthTime string
	Addtime    string
	Uptime     string
	ListOrder  string
}{
	ID:         "id",
	Name:       "name",
	Type:       "type",
	Coin:       "coin",
	LengthType: "length_type",
	Length:     "length",
	LengthTime: "length_time",
	Addtime:    "addtime",
	Uptime:     "uptime",
	ListOrder:  "list_order",
}

// Generated where

var CMFGuardWhere = struct {
	ID         whereHelperuint
	Name       whereHelperstring
	Type       whereHelperbool
	Coin       whereHelperint
	LengthType whereHelperbool
	Length     whereHelperint
	LengthTime whereHelperint
	Addtime    whereHelperint
	Uptime     whereHelperint
	ListOrder  whereHelperint
}{
	ID:         whereHelperuint{field: "`cmf_guard`.`id`"},
	Name:       whereHelperstring{field: "`cmf_guard`.`name`"},
	Type:       whereHelperbool{field: "`cmf_guard`.`type`"},
	Coin:       whereHelperint{field: "`cmf_guard`.`coin`"},
	LengthType: whereHelperbool{field: "`cmf_guard`.`length_type`"},
	Length:     whereHelperint{field: "`cmf_guard`.`length`"},
	LengthTime: whereHelperint{field: "`cmf_guard`.`length_time`"},
	Addtime:    whereHelperint{field: "`cmf_guard`.`addtime`"},
	Uptime:     whereHelperint{field: "`cmf_guard`.`uptime`"},
	ListOrder:  whereHelperint{field: "`cmf_guard`.`list_order`"},
}

// CMFGuardRels is where relationship names are stored.
var CMFGuardRels = struct {
}{}

// cmfGuardR is where relationships are stored.
type cmfGuardR struct {
}

// NewStruct creates a new relationship struct
func (*cmfGuardR) NewStruct() *cmfGuardR {
	return &cmfGuardR{}
}

// cmfGuardL is where Load methods for each relationship are stored.
type cmfGuardL struct{}

var (
	cmfGuardAllColumns            = []string{"id", "name", "type", "coin", "length_type", "length", "length_time", "addtime", "uptime", "list_order"}
	cmfGuardColumnsWithoutDefault = []string{"name"}
	cmfGuardColumnsWithDefault    = []string{"id", "type", "coin", "length_type", "length", "length_time", "addtime", "uptime", "list_order"}
	cmfGuardPrimaryKeyColumns     = []string{"id"}
)

type (
	// CMFGuardSlice is an alias for a slice of pointers to CMFGuard.
	// This should generally be used opposed to []CMFGuard.
	CMFGuardSlice []*CMFGuard
	// CMFGuardHook is the signature for custom CMFGuard hook methods
	CMFGuardHook func(context.Context, boil.ContextExecutor, *CMFGuard) error

	cmfGuardQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	cmfGuardType                 = reflect.TypeOf(&CMFGuard{})
	cmfGuardMapping              = queries.MakeStructMapping(cmfGuardType)
	cmfGuardPrimaryKeyMapping, _ = queries.BindMapping(cmfGuardType, cmfGuardMapping, cmfGuardPrimaryKeyColumns)
	cmfGuardInsertCacheMut       sync.RWMutex
	cmfGuardInsertCache          = make(map[string]insertCache)
	cmfGuardUpdateCacheMut       sync.RWMutex
	cmfGuardUpdateCache          = make(map[string]updateCache)
	cmfGuardUpsertCacheMut       sync.RWMutex
	cmfGuardUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var cmfGuardBeforeInsertHooks []CMFGuardHook
var cmfGuardBeforeUpdateHooks []CMFGuardHook
var cmfGuardBeforeDeleteHooks []CMFGuardHook
var cmfGuardBeforeUpsertHooks []CMFGuardHook

var cmfGuardAfterInsertHooks []CMFGuardHook
var cmfGuardAfterSelectHooks []CMFGuardHook
var cmfGuardAfterUpdateHooks []CMFGuardHook
var cmfGuardAfterDeleteHooks []CMFGuardHook
var cmfGuardAfterUpsertHooks []CMFGuardHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CMFGuard) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfGuardBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CMFGuard) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfGuardBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CMFGuard) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfGuardBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CMFGuard) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfGuardBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CMFGuard) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfGuardAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CMFGuard) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfGuardAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CMFGuard) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfGuardAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CMFGuard) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfGuardAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CMFGuard) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfGuardAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCMFGuardHook registers your hook function for all future operations.
func AddCMFGuardHook(hookPoint boil.HookPoint, cmfGuardHook CMFGuardHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		cmfGuardBeforeInsertHooks = append(cmfGuardBeforeInsertHooks, cmfGuardHook)
	case boil.BeforeUpdateHook:
		cmfGuardBeforeUpdateHooks = append(cmfGuardBeforeUpdateHooks, cmfGuardHook)
	case boil.BeforeDeleteHook:
		cmfGuardBeforeDeleteHooks = append(cmfGuardBeforeDeleteHooks, cmfGuardHook)
	case boil.BeforeUpsertHook:
		cmfGuardBeforeUpsertHooks = append(cmfGuardBeforeUpsertHooks, cmfGuardHook)
	case boil.AfterInsertHook:
		cmfGuardAfterInsertHooks = append(cmfGuardAfterInsertHooks, cmfGuardHook)
	case boil.AfterSelectHook:
		cmfGuardAfterSelectHooks = append(cmfGuardAfterSelectHooks, cmfGuardHook)
	case boil.AfterUpdateHook:
		cmfGuardAfterUpdateHooks = append(cmfGuardAfterUpdateHooks, cmfGuardHook)
	case boil.AfterDeleteHook:
		cmfGuardAfterDeleteHooks = append(cmfGuardAfterDeleteHooks, cmfGuardHook)
	case boil.AfterUpsertHook:
		cmfGuardAfterUpsertHooks = append(cmfGuardAfterUpsertHooks, cmfGuardHook)
	}
}

// One returns a single cmfGuard record from the query.
func (q cmfGuardQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CMFGuard, error) {
	o := &CMFGuard{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for cmf_guard")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all CMFGuard records from the query.
func (q cmfGuardQuery) All(ctx context.Context, exec boil.ContextExecutor) (CMFGuardSlice, error) {
	var o []*CMFGuard

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CMFGuard slice")
	}

	if len(cmfGuardAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all CMFGuard records in the query.
func (q cmfGuardQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count cmf_guard rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q cmfGuardQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if cmf_guard exists")
	}

	return count > 0, nil
}

// CMFGuards retrieves all the records using an executor.
func CMFGuards(mods ...qm.QueryMod) cmfGuardQuery {
	mods = append(mods, qm.From("`cmf_guard`"))
	return cmfGuardQuery{NewQuery(mods...)}
}

// FindCMFGuard retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCMFGuard(ctx context.Context, exec boil.ContextExecutor, iD uint, selectCols ...string) (*CMFGuard, error) {
	cmfGuardObj := &CMFGuard{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `cmf_guard` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, cmfGuardObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from cmf_guard")
	}

	return cmfGuardObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CMFGuard) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cmf_guard provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(cmfGuardColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	cmfGuardInsertCacheMut.RLock()
	cache, cached := cmfGuardInsertCache[key]
	cmfGuardInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			cmfGuardAllColumns,
			cmfGuardColumnsWithDefault,
			cmfGuardColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(cmfGuardType, cmfGuardMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(cmfGuardType, cmfGuardMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `cmf_guard` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `cmf_guard` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `cmf_guard` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, cmfGuardPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into cmf_guard")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == cmfGuardMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cmf_guard")
	}

CacheNoHooks:
	if !cached {
		cmfGuardInsertCacheMut.Lock()
		cmfGuardInsertCache[key] = cache
		cmfGuardInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the CMFGuard.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CMFGuard) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	cmfGuardUpdateCacheMut.RLock()
	cache, cached := cmfGuardUpdateCache[key]
	cmfGuardUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			cmfGuardAllColumns,
			cmfGuardPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update cmf_guard, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `cmf_guard` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, cmfGuardPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(cmfGuardType, cmfGuardMapping, append(wl, cmfGuardPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update cmf_guard row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for cmf_guard")
	}

	if !cached {
		cmfGuardUpdateCacheMut.Lock()
		cmfGuardUpdateCache[key] = cache
		cmfGuardUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q cmfGuardQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for cmf_guard")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for cmf_guard")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CMFGuardSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfGuardPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `cmf_guard` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfGuardPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in cmfGuard slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all cmfGuard")
	}
	return rowsAff, nil
}

var mySQLCMFGuardUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CMFGuard) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cmf_guard provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(cmfGuardColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLCMFGuardUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	cmfGuardUpsertCacheMut.RLock()
	cache, cached := cmfGuardUpsertCache[key]
	cmfGuardUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			cmfGuardAllColumns,
			cmfGuardColumnsWithDefault,
			cmfGuardColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			cmfGuardAllColumns,
			cmfGuardPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert cmf_guard, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`cmf_guard`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `cmf_guard` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(cmfGuardType, cmfGuardMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(cmfGuardType, cmfGuardMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for cmf_guard")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == cmfGuardMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(cmfGuardType, cmfGuardMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for cmf_guard")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cmf_guard")
	}

CacheNoHooks:
	if !cached {
		cmfGuardUpsertCacheMut.Lock()
		cmfGuardUpsertCache[key] = cache
		cmfGuardUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single CMFGuard record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CMFGuard) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CMFGuard provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cmfGuardPrimaryKeyMapping)
	sql := "DELETE FROM `cmf_guard` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from cmf_guard")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for cmf_guard")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q cmfGuardQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no cmfGuardQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cmf_guard")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cmf_guard")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CMFGuardSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(cmfGuardBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfGuardPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `cmf_guard` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfGuardPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cmfGuard slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cmf_guard")
	}

	if len(cmfGuardAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CMFGuard) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCMFGuard(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CMFGuardSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CMFGuardSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfGuardPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `cmf_guard`.* FROM `cmf_guard` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfGuardPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CMFGuardSlice")
	}

	*o = slice

	return nil
}

// CMFGuardExists checks if the CMFGuard row exists.
func CMFGuardExists(ctx context.Context, exec boil.ContextExecutor, iD uint) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `cmf_guard` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if cmf_guard exists")
	}

	return exists, nil
}
