// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// CMFPaidprogramClass is an object representing the database table.
type CMFPaidprogramClass struct {
	ID        int    `boil:"id" json:"id" toml:"id" yaml:"id"`
	Name      string `boil:"name" json:"name" toml:"name" yaml:"name"`
	ListOrder int    `boil:"list_order" json:"list_order" toml:"list_order" yaml:"list_order"`
	Status    bool   `boil:"status" json:"status" toml:"status" yaml:"status"`
	Addtime   int    `boil:"addtime" json:"addtime" toml:"addtime" yaml:"addtime"`
	Edittime  int    `boil:"edittime" json:"edittime" toml:"edittime" yaml:"edittime"`

	R *cmfPaidprogramClassR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L cmfPaidprogramClassL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CMFPaidprogramClassColumns = struct {
	ID        string
	Name      string
	ListOrder string
	Status    string
	Addtime   string
	Edittime  string
}{
	ID:        "id",
	Name:      "name",
	ListOrder: "list_order",
	Status:    "status",
	Addtime:   "addtime",
	Edittime:  "edittime",
}

// Generated where

var CMFPaidprogramClassWhere = struct {
	ID        whereHelperint
	Name      whereHelperstring
	ListOrder whereHelperint
	Status    whereHelperbool
	Addtime   whereHelperint
	Edittime  whereHelperint
}{
	ID:        whereHelperint{field: "`cmf_paidprogram_class`.`id`"},
	Name:      whereHelperstring{field: "`cmf_paidprogram_class`.`name`"},
	ListOrder: whereHelperint{field: "`cmf_paidprogram_class`.`list_order`"},
	Status:    whereHelperbool{field: "`cmf_paidprogram_class`.`status`"},
	Addtime:   whereHelperint{field: "`cmf_paidprogram_class`.`addtime`"},
	Edittime:  whereHelperint{field: "`cmf_paidprogram_class`.`edittime`"},
}

// CMFPaidprogramClassRels is where relationship names are stored.
var CMFPaidprogramClassRels = struct {
}{}

// cmfPaidprogramClassR is where relationships are stored.
type cmfPaidprogramClassR struct {
}

// NewStruct creates a new relationship struct
func (*cmfPaidprogramClassR) NewStruct() *cmfPaidprogramClassR {
	return &cmfPaidprogramClassR{}
}

// cmfPaidprogramClassL is where Load methods for each relationship are stored.
type cmfPaidprogramClassL struct{}

var (
	cmfPaidprogramClassAllColumns            = []string{"id", "name", "list_order", "status", "addtime", "edittime"}
	cmfPaidprogramClassColumnsWithoutDefault = []string{"name"}
	cmfPaidprogramClassColumnsWithDefault    = []string{"id", "list_order", "status", "addtime", "edittime"}
	cmfPaidprogramClassPrimaryKeyColumns     = []string{"id"}
)

type (
	// CMFPaidprogramClassSlice is an alias for a slice of pointers to CMFPaidprogramClass.
	// This should generally be used opposed to []CMFPaidprogramClass.
	CMFPaidprogramClassSlice []*CMFPaidprogramClass
	// CMFPaidprogramClassHook is the signature for custom CMFPaidprogramClass hook methods
	CMFPaidprogramClassHook func(context.Context, boil.ContextExecutor, *CMFPaidprogramClass) error

	cmfPaidprogramClassQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	cmfPaidprogramClassType                 = reflect.TypeOf(&CMFPaidprogramClass{})
	cmfPaidprogramClassMapping              = queries.MakeStructMapping(cmfPaidprogramClassType)
	cmfPaidprogramClassPrimaryKeyMapping, _ = queries.BindMapping(cmfPaidprogramClassType, cmfPaidprogramClassMapping, cmfPaidprogramClassPrimaryKeyColumns)
	cmfPaidprogramClassInsertCacheMut       sync.RWMutex
	cmfPaidprogramClassInsertCache          = make(map[string]insertCache)
	cmfPaidprogramClassUpdateCacheMut       sync.RWMutex
	cmfPaidprogramClassUpdateCache          = make(map[string]updateCache)
	cmfPaidprogramClassUpsertCacheMut       sync.RWMutex
	cmfPaidprogramClassUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var cmfPaidprogramClassBeforeInsertHooks []CMFPaidprogramClassHook
var cmfPaidprogramClassBeforeUpdateHooks []CMFPaidprogramClassHook
var cmfPaidprogramClassBeforeDeleteHooks []CMFPaidprogramClassHook
var cmfPaidprogramClassBeforeUpsertHooks []CMFPaidprogramClassHook

var cmfPaidprogramClassAfterInsertHooks []CMFPaidprogramClassHook
var cmfPaidprogramClassAfterSelectHooks []CMFPaidprogramClassHook
var cmfPaidprogramClassAfterUpdateHooks []CMFPaidprogramClassHook
var cmfPaidprogramClassAfterDeleteHooks []CMFPaidprogramClassHook
var cmfPaidprogramClassAfterUpsertHooks []CMFPaidprogramClassHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CMFPaidprogramClass) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfPaidprogramClassBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CMFPaidprogramClass) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfPaidprogramClassBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CMFPaidprogramClass) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfPaidprogramClassBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CMFPaidprogramClass) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfPaidprogramClassBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CMFPaidprogramClass) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfPaidprogramClassAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CMFPaidprogramClass) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfPaidprogramClassAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CMFPaidprogramClass) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfPaidprogramClassAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CMFPaidprogramClass) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfPaidprogramClassAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CMFPaidprogramClass) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfPaidprogramClassAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCMFPaidprogramClassHook registers your hook function for all future operations.
func AddCMFPaidprogramClassHook(hookPoint boil.HookPoint, cmfPaidprogramClassHook CMFPaidprogramClassHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		cmfPaidprogramClassBeforeInsertHooks = append(cmfPaidprogramClassBeforeInsertHooks, cmfPaidprogramClassHook)
	case boil.BeforeUpdateHook:
		cmfPaidprogramClassBeforeUpdateHooks = append(cmfPaidprogramClassBeforeUpdateHooks, cmfPaidprogramClassHook)
	case boil.BeforeDeleteHook:
		cmfPaidprogramClassBeforeDeleteHooks = append(cmfPaidprogramClassBeforeDeleteHooks, cmfPaidprogramClassHook)
	case boil.BeforeUpsertHook:
		cmfPaidprogramClassBeforeUpsertHooks = append(cmfPaidprogramClassBeforeUpsertHooks, cmfPaidprogramClassHook)
	case boil.AfterInsertHook:
		cmfPaidprogramClassAfterInsertHooks = append(cmfPaidprogramClassAfterInsertHooks, cmfPaidprogramClassHook)
	case boil.AfterSelectHook:
		cmfPaidprogramClassAfterSelectHooks = append(cmfPaidprogramClassAfterSelectHooks, cmfPaidprogramClassHook)
	case boil.AfterUpdateHook:
		cmfPaidprogramClassAfterUpdateHooks = append(cmfPaidprogramClassAfterUpdateHooks, cmfPaidprogramClassHook)
	case boil.AfterDeleteHook:
		cmfPaidprogramClassAfterDeleteHooks = append(cmfPaidprogramClassAfterDeleteHooks, cmfPaidprogramClassHook)
	case boil.AfterUpsertHook:
		cmfPaidprogramClassAfterUpsertHooks = append(cmfPaidprogramClassAfterUpsertHooks, cmfPaidprogramClassHook)
	}
}

// One returns a single cmfPaidprogramClass record from the query.
func (q cmfPaidprogramClassQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CMFPaidprogramClass, error) {
	o := &CMFPaidprogramClass{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for cmf_paidprogram_class")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all CMFPaidprogramClass records from the query.
func (q cmfPaidprogramClassQuery) All(ctx context.Context, exec boil.ContextExecutor) (CMFPaidprogramClassSlice, error) {
	var o []*CMFPaidprogramClass

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CMFPaidprogramClass slice")
	}

	if len(cmfPaidprogramClassAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all CMFPaidprogramClass records in the query.
func (q cmfPaidprogramClassQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count cmf_paidprogram_class rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q cmfPaidprogramClassQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if cmf_paidprogram_class exists")
	}

	return count > 0, nil
}

// CMFPaidprogramClasses retrieves all the records using an executor.
func CMFPaidprogramClasses(mods ...qm.QueryMod) cmfPaidprogramClassQuery {
	mods = append(mods, qm.From("`cmf_paidprogram_class`"))
	return cmfPaidprogramClassQuery{NewQuery(mods...)}
}

// FindCMFPaidprogramClass retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCMFPaidprogramClass(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*CMFPaidprogramClass, error) {
	cmfPaidprogramClassObj := &CMFPaidprogramClass{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `cmf_paidprogram_class` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, cmfPaidprogramClassObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from cmf_paidprogram_class")
	}

	return cmfPaidprogramClassObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CMFPaidprogramClass) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cmf_paidprogram_class provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(cmfPaidprogramClassColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	cmfPaidprogramClassInsertCacheMut.RLock()
	cache, cached := cmfPaidprogramClassInsertCache[key]
	cmfPaidprogramClassInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			cmfPaidprogramClassAllColumns,
			cmfPaidprogramClassColumnsWithDefault,
			cmfPaidprogramClassColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(cmfPaidprogramClassType, cmfPaidprogramClassMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(cmfPaidprogramClassType, cmfPaidprogramClassMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `cmf_paidprogram_class` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `cmf_paidprogram_class` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `cmf_paidprogram_class` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, cmfPaidprogramClassPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into cmf_paidprogram_class")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == cmfPaidprogramClassMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cmf_paidprogram_class")
	}

CacheNoHooks:
	if !cached {
		cmfPaidprogramClassInsertCacheMut.Lock()
		cmfPaidprogramClassInsertCache[key] = cache
		cmfPaidprogramClassInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the CMFPaidprogramClass.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CMFPaidprogramClass) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	cmfPaidprogramClassUpdateCacheMut.RLock()
	cache, cached := cmfPaidprogramClassUpdateCache[key]
	cmfPaidprogramClassUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			cmfPaidprogramClassAllColumns,
			cmfPaidprogramClassPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update cmf_paidprogram_class, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `cmf_paidprogram_class` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, cmfPaidprogramClassPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(cmfPaidprogramClassType, cmfPaidprogramClassMapping, append(wl, cmfPaidprogramClassPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update cmf_paidprogram_class row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for cmf_paidprogram_class")
	}

	if !cached {
		cmfPaidprogramClassUpdateCacheMut.Lock()
		cmfPaidprogramClassUpdateCache[key] = cache
		cmfPaidprogramClassUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q cmfPaidprogramClassQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for cmf_paidprogram_class")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for cmf_paidprogram_class")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CMFPaidprogramClassSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfPaidprogramClassPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `cmf_paidprogram_class` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfPaidprogramClassPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in cmfPaidprogramClass slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all cmfPaidprogramClass")
	}
	return rowsAff, nil
}

var mySQLCMFPaidprogramClassUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CMFPaidprogramClass) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cmf_paidprogram_class provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(cmfPaidprogramClassColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLCMFPaidprogramClassUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	cmfPaidprogramClassUpsertCacheMut.RLock()
	cache, cached := cmfPaidprogramClassUpsertCache[key]
	cmfPaidprogramClassUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			cmfPaidprogramClassAllColumns,
			cmfPaidprogramClassColumnsWithDefault,
			cmfPaidprogramClassColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			cmfPaidprogramClassAllColumns,
			cmfPaidprogramClassPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert cmf_paidprogram_class, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`cmf_paidprogram_class`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `cmf_paidprogram_class` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(cmfPaidprogramClassType, cmfPaidprogramClassMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(cmfPaidprogramClassType, cmfPaidprogramClassMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for cmf_paidprogram_class")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == cmfPaidprogramClassMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(cmfPaidprogramClassType, cmfPaidprogramClassMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for cmf_paidprogram_class")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cmf_paidprogram_class")
	}

CacheNoHooks:
	if !cached {
		cmfPaidprogramClassUpsertCacheMut.Lock()
		cmfPaidprogramClassUpsertCache[key] = cache
		cmfPaidprogramClassUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single CMFPaidprogramClass record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CMFPaidprogramClass) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CMFPaidprogramClass provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cmfPaidprogramClassPrimaryKeyMapping)
	sql := "DELETE FROM `cmf_paidprogram_class` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from cmf_paidprogram_class")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for cmf_paidprogram_class")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q cmfPaidprogramClassQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no cmfPaidprogramClassQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cmf_paidprogram_class")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cmf_paidprogram_class")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CMFPaidprogramClassSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(cmfPaidprogramClassBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfPaidprogramClassPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `cmf_paidprogram_class` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfPaidprogramClassPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cmfPaidprogramClass slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cmf_paidprogram_class")
	}

	if len(cmfPaidprogramClassAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CMFPaidprogramClass) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCMFPaidprogramClass(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CMFPaidprogramClassSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CMFPaidprogramClassSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfPaidprogramClassPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `cmf_paidprogram_class`.* FROM `cmf_paidprogram_class` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfPaidprogramClassPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CMFPaidprogramClassSlice")
	}

	*o = slice

	return nil
}

// CMFPaidprogramClassExists checks if the CMFPaidprogramClass row exists.
func CMFPaidprogramClassExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `cmf_paidprogram_class` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if cmf_paidprogram_class exists")
	}

	return exists, nil
}
