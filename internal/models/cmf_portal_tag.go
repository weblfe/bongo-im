// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// CMFPortalTag is an object representing the database table.
type CMFPortalTag struct {
	ID          uint64 `boil:"id" json:"id" toml:"id" yaml:"id"`
	Status      uint8  `boil:"status" json:"status" toml:"status" yaml:"status"`
	Recommended uint8  `boil:"recommended" json:"recommended" toml:"recommended" yaml:"recommended"`
	PostCount   uint64 `boil:"post_count" json:"post_count" toml:"post_count" yaml:"post_count"`
	Name        string `boil:"name" json:"name" toml:"name" yaml:"name"`

	R *cmfPortalTagR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L cmfPortalTagL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CMFPortalTagColumns = struct {
	ID          string
	Status      string
	Recommended string
	PostCount   string
	Name        string
}{
	ID:          "id",
	Status:      "status",
	Recommended: "recommended",
	PostCount:   "post_count",
	Name:        "name",
}

// Generated where

var CMFPortalTagWhere = struct {
	ID          whereHelperuint64
	Status      whereHelperuint8
	Recommended whereHelperuint8
	PostCount   whereHelperuint64
	Name        whereHelperstring
}{
	ID:          whereHelperuint64{field: "`cmf_portal_tag`.`id`"},
	Status:      whereHelperuint8{field: "`cmf_portal_tag`.`status`"},
	Recommended: whereHelperuint8{field: "`cmf_portal_tag`.`recommended`"},
	PostCount:   whereHelperuint64{field: "`cmf_portal_tag`.`post_count`"},
	Name:        whereHelperstring{field: "`cmf_portal_tag`.`name`"},
}

// CMFPortalTagRels is where relationship names are stored.
var CMFPortalTagRels = struct {
}{}

// cmfPortalTagR is where relationships are stored.
type cmfPortalTagR struct {
}

// NewStruct creates a new relationship struct
func (*cmfPortalTagR) NewStruct() *cmfPortalTagR {
	return &cmfPortalTagR{}
}

// cmfPortalTagL is where Load methods for each relationship are stored.
type cmfPortalTagL struct{}

var (
	cmfPortalTagAllColumns            = []string{"id", "status", "recommended", "post_count", "name"}
	cmfPortalTagColumnsWithoutDefault = []string{"name"}
	cmfPortalTagColumnsWithDefault    = []string{"id", "status", "recommended", "post_count"}
	cmfPortalTagPrimaryKeyColumns     = []string{"id"}
)

type (
	// CMFPortalTagSlice is an alias for a slice of pointers to CMFPortalTag.
	// This should generally be used opposed to []CMFPortalTag.
	CMFPortalTagSlice []*CMFPortalTag
	// CMFPortalTagHook is the signature for custom CMFPortalTag hook methods
	CMFPortalTagHook func(context.Context, boil.ContextExecutor, *CMFPortalTag) error

	cmfPortalTagQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	cmfPortalTagType                 = reflect.TypeOf(&CMFPortalTag{})
	cmfPortalTagMapping              = queries.MakeStructMapping(cmfPortalTagType)
	cmfPortalTagPrimaryKeyMapping, _ = queries.BindMapping(cmfPortalTagType, cmfPortalTagMapping, cmfPortalTagPrimaryKeyColumns)
	cmfPortalTagInsertCacheMut       sync.RWMutex
	cmfPortalTagInsertCache          = make(map[string]insertCache)
	cmfPortalTagUpdateCacheMut       sync.RWMutex
	cmfPortalTagUpdateCache          = make(map[string]updateCache)
	cmfPortalTagUpsertCacheMut       sync.RWMutex
	cmfPortalTagUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var cmfPortalTagBeforeInsertHooks []CMFPortalTagHook
var cmfPortalTagBeforeUpdateHooks []CMFPortalTagHook
var cmfPortalTagBeforeDeleteHooks []CMFPortalTagHook
var cmfPortalTagBeforeUpsertHooks []CMFPortalTagHook

var cmfPortalTagAfterInsertHooks []CMFPortalTagHook
var cmfPortalTagAfterSelectHooks []CMFPortalTagHook
var cmfPortalTagAfterUpdateHooks []CMFPortalTagHook
var cmfPortalTagAfterDeleteHooks []CMFPortalTagHook
var cmfPortalTagAfterUpsertHooks []CMFPortalTagHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CMFPortalTag) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfPortalTagBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CMFPortalTag) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfPortalTagBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CMFPortalTag) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfPortalTagBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CMFPortalTag) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfPortalTagBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CMFPortalTag) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfPortalTagAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CMFPortalTag) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfPortalTagAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CMFPortalTag) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfPortalTagAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CMFPortalTag) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfPortalTagAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CMFPortalTag) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfPortalTagAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCMFPortalTagHook registers your hook function for all future operations.
func AddCMFPortalTagHook(hookPoint boil.HookPoint, cmfPortalTagHook CMFPortalTagHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		cmfPortalTagBeforeInsertHooks = append(cmfPortalTagBeforeInsertHooks, cmfPortalTagHook)
	case boil.BeforeUpdateHook:
		cmfPortalTagBeforeUpdateHooks = append(cmfPortalTagBeforeUpdateHooks, cmfPortalTagHook)
	case boil.BeforeDeleteHook:
		cmfPortalTagBeforeDeleteHooks = append(cmfPortalTagBeforeDeleteHooks, cmfPortalTagHook)
	case boil.BeforeUpsertHook:
		cmfPortalTagBeforeUpsertHooks = append(cmfPortalTagBeforeUpsertHooks, cmfPortalTagHook)
	case boil.AfterInsertHook:
		cmfPortalTagAfterInsertHooks = append(cmfPortalTagAfterInsertHooks, cmfPortalTagHook)
	case boil.AfterSelectHook:
		cmfPortalTagAfterSelectHooks = append(cmfPortalTagAfterSelectHooks, cmfPortalTagHook)
	case boil.AfterUpdateHook:
		cmfPortalTagAfterUpdateHooks = append(cmfPortalTagAfterUpdateHooks, cmfPortalTagHook)
	case boil.AfterDeleteHook:
		cmfPortalTagAfterDeleteHooks = append(cmfPortalTagAfterDeleteHooks, cmfPortalTagHook)
	case boil.AfterUpsertHook:
		cmfPortalTagAfterUpsertHooks = append(cmfPortalTagAfterUpsertHooks, cmfPortalTagHook)
	}
}

// One returns a single cmfPortalTag record from the query.
func (q cmfPortalTagQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CMFPortalTag, error) {
	o := &CMFPortalTag{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for cmf_portal_tag")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all CMFPortalTag records from the query.
func (q cmfPortalTagQuery) All(ctx context.Context, exec boil.ContextExecutor) (CMFPortalTagSlice, error) {
	var o []*CMFPortalTag

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CMFPortalTag slice")
	}

	if len(cmfPortalTagAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all CMFPortalTag records in the query.
func (q cmfPortalTagQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count cmf_portal_tag rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q cmfPortalTagQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if cmf_portal_tag exists")
	}

	return count > 0, nil
}

// CMFPortalTags retrieves all the records using an executor.
func CMFPortalTags(mods ...qm.QueryMod) cmfPortalTagQuery {
	mods = append(mods, qm.From("`cmf_portal_tag`"))
	return cmfPortalTagQuery{NewQuery(mods...)}
}

// FindCMFPortalTag retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCMFPortalTag(ctx context.Context, exec boil.ContextExecutor, iD uint64, selectCols ...string) (*CMFPortalTag, error) {
	cmfPortalTagObj := &CMFPortalTag{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `cmf_portal_tag` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, cmfPortalTagObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from cmf_portal_tag")
	}

	return cmfPortalTagObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CMFPortalTag) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cmf_portal_tag provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(cmfPortalTagColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	cmfPortalTagInsertCacheMut.RLock()
	cache, cached := cmfPortalTagInsertCache[key]
	cmfPortalTagInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			cmfPortalTagAllColumns,
			cmfPortalTagColumnsWithDefault,
			cmfPortalTagColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(cmfPortalTagType, cmfPortalTagMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(cmfPortalTagType, cmfPortalTagMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `cmf_portal_tag` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `cmf_portal_tag` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `cmf_portal_tag` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, cmfPortalTagPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into cmf_portal_tag")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == cmfPortalTagMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cmf_portal_tag")
	}

CacheNoHooks:
	if !cached {
		cmfPortalTagInsertCacheMut.Lock()
		cmfPortalTagInsertCache[key] = cache
		cmfPortalTagInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the CMFPortalTag.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CMFPortalTag) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	cmfPortalTagUpdateCacheMut.RLock()
	cache, cached := cmfPortalTagUpdateCache[key]
	cmfPortalTagUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			cmfPortalTagAllColumns,
			cmfPortalTagPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update cmf_portal_tag, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `cmf_portal_tag` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, cmfPortalTagPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(cmfPortalTagType, cmfPortalTagMapping, append(wl, cmfPortalTagPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update cmf_portal_tag row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for cmf_portal_tag")
	}

	if !cached {
		cmfPortalTagUpdateCacheMut.Lock()
		cmfPortalTagUpdateCache[key] = cache
		cmfPortalTagUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q cmfPortalTagQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for cmf_portal_tag")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for cmf_portal_tag")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CMFPortalTagSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfPortalTagPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `cmf_portal_tag` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfPortalTagPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in cmfPortalTag slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all cmfPortalTag")
	}
	return rowsAff, nil
}

var mySQLCMFPortalTagUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CMFPortalTag) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cmf_portal_tag provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(cmfPortalTagColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLCMFPortalTagUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	cmfPortalTagUpsertCacheMut.RLock()
	cache, cached := cmfPortalTagUpsertCache[key]
	cmfPortalTagUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			cmfPortalTagAllColumns,
			cmfPortalTagColumnsWithDefault,
			cmfPortalTagColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			cmfPortalTagAllColumns,
			cmfPortalTagPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert cmf_portal_tag, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`cmf_portal_tag`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `cmf_portal_tag` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(cmfPortalTagType, cmfPortalTagMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(cmfPortalTagType, cmfPortalTagMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for cmf_portal_tag")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == cmfPortalTagMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(cmfPortalTagType, cmfPortalTagMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for cmf_portal_tag")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cmf_portal_tag")
	}

CacheNoHooks:
	if !cached {
		cmfPortalTagUpsertCacheMut.Lock()
		cmfPortalTagUpsertCache[key] = cache
		cmfPortalTagUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single CMFPortalTag record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CMFPortalTag) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CMFPortalTag provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cmfPortalTagPrimaryKeyMapping)
	sql := "DELETE FROM `cmf_portal_tag` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from cmf_portal_tag")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for cmf_portal_tag")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q cmfPortalTagQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no cmfPortalTagQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cmf_portal_tag")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cmf_portal_tag")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CMFPortalTagSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(cmfPortalTagBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfPortalTagPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `cmf_portal_tag` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfPortalTagPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cmfPortalTag slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cmf_portal_tag")
	}

	if len(cmfPortalTagAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CMFPortalTag) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCMFPortalTag(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CMFPortalTagSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CMFPortalTagSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfPortalTagPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `cmf_portal_tag`.* FROM `cmf_portal_tag` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfPortalTagPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CMFPortalTagSlice")
	}

	*o = slice

	return nil
}

// CMFPortalTagExists checks if the CMFPortalTag row exists.
func CMFPortalTagExists(ctx context.Context, exec boil.ContextExecutor, iD uint64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `cmf_portal_tag` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if cmf_portal_tag exists")
	}

	return exists, nil
}
