// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// CMFTurntable is an object representing the database table.
type CMFTurntable struct {
	ID      uint          `boil:"id" json:"id" toml:"id" yaml:"id"`
	Type    bool          `boil:"type" json:"type" toml:"type" yaml:"type"`
	TypeVal string        `boil:"type_val" json:"type_val" toml:"type_val" yaml:"type_val"`
	Thumb   string        `boil:"thumb" json:"thumb" toml:"thumb" yaml:"thumb"`
	Rate    types.Decimal `boil:"rate" json:"rate" toml:"rate" yaml:"rate"`
	Uptime  int64         `boil:"uptime" json:"uptime" toml:"uptime" yaml:"uptime"`

	R *cmfTurntableR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L cmfTurntableL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CMFTurntableColumns = struct {
	ID      string
	Type    string
	TypeVal string
	Thumb   string
	Rate    string
	Uptime  string
}{
	ID:      "id",
	Type:    "type",
	TypeVal: "type_val",
	Thumb:   "thumb",
	Rate:    "rate",
	Uptime:  "uptime",
}

// Generated where

var CMFTurntableWhere = struct {
	ID      whereHelperuint
	Type    whereHelperbool
	TypeVal whereHelperstring
	Thumb   whereHelperstring
	Rate    whereHelpertypes_Decimal
	Uptime  whereHelperint64
}{
	ID:      whereHelperuint{field: "`cmf_turntable`.`id`"},
	Type:    whereHelperbool{field: "`cmf_turntable`.`type`"},
	TypeVal: whereHelperstring{field: "`cmf_turntable`.`type_val`"},
	Thumb:   whereHelperstring{field: "`cmf_turntable`.`thumb`"},
	Rate:    whereHelpertypes_Decimal{field: "`cmf_turntable`.`rate`"},
	Uptime:  whereHelperint64{field: "`cmf_turntable`.`uptime`"},
}

// CMFTurntableRels is where relationship names are stored.
var CMFTurntableRels = struct {
}{}

// cmfTurntableR is where relationships are stored.
type cmfTurntableR struct {
}

// NewStruct creates a new relationship struct
func (*cmfTurntableR) NewStruct() *cmfTurntableR {
	return &cmfTurntableR{}
}

// cmfTurntableL is where Load methods for each relationship are stored.
type cmfTurntableL struct{}

var (
	cmfTurntableAllColumns            = []string{"id", "type", "type_val", "thumb", "rate", "uptime"}
	cmfTurntableColumnsWithoutDefault = []string{"type_val", "thumb"}
	cmfTurntableColumnsWithDefault    = []string{"id", "type", "rate", "uptime"}
	cmfTurntablePrimaryKeyColumns     = []string{"id"}
)

type (
	// CMFTurntableSlice is an alias for a slice of pointers to CMFTurntable.
	// This should generally be used opposed to []CMFTurntable.
	CMFTurntableSlice []*CMFTurntable
	// CMFTurntableHook is the signature for custom CMFTurntable hook methods
	CMFTurntableHook func(context.Context, boil.ContextExecutor, *CMFTurntable) error

	cmfTurntableQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	cmfTurntableType                 = reflect.TypeOf(&CMFTurntable{})
	cmfTurntableMapping              = queries.MakeStructMapping(cmfTurntableType)
	cmfTurntablePrimaryKeyMapping, _ = queries.BindMapping(cmfTurntableType, cmfTurntableMapping, cmfTurntablePrimaryKeyColumns)
	cmfTurntableInsertCacheMut       sync.RWMutex
	cmfTurntableInsertCache          = make(map[string]insertCache)
	cmfTurntableUpdateCacheMut       sync.RWMutex
	cmfTurntableUpdateCache          = make(map[string]updateCache)
	cmfTurntableUpsertCacheMut       sync.RWMutex
	cmfTurntableUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var cmfTurntableBeforeInsertHooks []CMFTurntableHook
var cmfTurntableBeforeUpdateHooks []CMFTurntableHook
var cmfTurntableBeforeDeleteHooks []CMFTurntableHook
var cmfTurntableBeforeUpsertHooks []CMFTurntableHook

var cmfTurntableAfterInsertHooks []CMFTurntableHook
var cmfTurntableAfterSelectHooks []CMFTurntableHook
var cmfTurntableAfterUpdateHooks []CMFTurntableHook
var cmfTurntableAfterDeleteHooks []CMFTurntableHook
var cmfTurntableAfterUpsertHooks []CMFTurntableHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CMFTurntable) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfTurntableBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CMFTurntable) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfTurntableBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CMFTurntable) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfTurntableBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CMFTurntable) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfTurntableBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CMFTurntable) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfTurntableAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CMFTurntable) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfTurntableAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CMFTurntable) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfTurntableAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CMFTurntable) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfTurntableAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CMFTurntable) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfTurntableAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCMFTurntableHook registers your hook function for all future operations.
func AddCMFTurntableHook(hookPoint boil.HookPoint, cmfTurntableHook CMFTurntableHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		cmfTurntableBeforeInsertHooks = append(cmfTurntableBeforeInsertHooks, cmfTurntableHook)
	case boil.BeforeUpdateHook:
		cmfTurntableBeforeUpdateHooks = append(cmfTurntableBeforeUpdateHooks, cmfTurntableHook)
	case boil.BeforeDeleteHook:
		cmfTurntableBeforeDeleteHooks = append(cmfTurntableBeforeDeleteHooks, cmfTurntableHook)
	case boil.BeforeUpsertHook:
		cmfTurntableBeforeUpsertHooks = append(cmfTurntableBeforeUpsertHooks, cmfTurntableHook)
	case boil.AfterInsertHook:
		cmfTurntableAfterInsertHooks = append(cmfTurntableAfterInsertHooks, cmfTurntableHook)
	case boil.AfterSelectHook:
		cmfTurntableAfterSelectHooks = append(cmfTurntableAfterSelectHooks, cmfTurntableHook)
	case boil.AfterUpdateHook:
		cmfTurntableAfterUpdateHooks = append(cmfTurntableAfterUpdateHooks, cmfTurntableHook)
	case boil.AfterDeleteHook:
		cmfTurntableAfterDeleteHooks = append(cmfTurntableAfterDeleteHooks, cmfTurntableHook)
	case boil.AfterUpsertHook:
		cmfTurntableAfterUpsertHooks = append(cmfTurntableAfterUpsertHooks, cmfTurntableHook)
	}
}

// One returns a single cmfTurntable record from the query.
func (q cmfTurntableQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CMFTurntable, error) {
	o := &CMFTurntable{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for cmf_turntable")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all CMFTurntable records from the query.
func (q cmfTurntableQuery) All(ctx context.Context, exec boil.ContextExecutor) (CMFTurntableSlice, error) {
	var o []*CMFTurntable

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CMFTurntable slice")
	}

	if len(cmfTurntableAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all CMFTurntable records in the query.
func (q cmfTurntableQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count cmf_turntable rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q cmfTurntableQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if cmf_turntable exists")
	}

	return count > 0, nil
}

// CMFTurntables retrieves all the records using an executor.
func CMFTurntables(mods ...qm.QueryMod) cmfTurntableQuery {
	mods = append(mods, qm.From("`cmf_turntable`"))
	return cmfTurntableQuery{NewQuery(mods...)}
}

// FindCMFTurntable retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCMFTurntable(ctx context.Context, exec boil.ContextExecutor, iD uint, selectCols ...string) (*CMFTurntable, error) {
	cmfTurntableObj := &CMFTurntable{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `cmf_turntable` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, cmfTurntableObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from cmf_turntable")
	}

	return cmfTurntableObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CMFTurntable) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cmf_turntable provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(cmfTurntableColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	cmfTurntableInsertCacheMut.RLock()
	cache, cached := cmfTurntableInsertCache[key]
	cmfTurntableInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			cmfTurntableAllColumns,
			cmfTurntableColumnsWithDefault,
			cmfTurntableColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(cmfTurntableType, cmfTurntableMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(cmfTurntableType, cmfTurntableMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `cmf_turntable` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `cmf_turntable` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `cmf_turntable` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, cmfTurntablePrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into cmf_turntable")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == cmfTurntableMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cmf_turntable")
	}

CacheNoHooks:
	if !cached {
		cmfTurntableInsertCacheMut.Lock()
		cmfTurntableInsertCache[key] = cache
		cmfTurntableInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the CMFTurntable.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CMFTurntable) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	cmfTurntableUpdateCacheMut.RLock()
	cache, cached := cmfTurntableUpdateCache[key]
	cmfTurntableUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			cmfTurntableAllColumns,
			cmfTurntablePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update cmf_turntable, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `cmf_turntable` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, cmfTurntablePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(cmfTurntableType, cmfTurntableMapping, append(wl, cmfTurntablePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update cmf_turntable row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for cmf_turntable")
	}

	if !cached {
		cmfTurntableUpdateCacheMut.Lock()
		cmfTurntableUpdateCache[key] = cache
		cmfTurntableUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q cmfTurntableQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for cmf_turntable")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for cmf_turntable")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CMFTurntableSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfTurntablePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `cmf_turntable` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfTurntablePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in cmfTurntable slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all cmfTurntable")
	}
	return rowsAff, nil
}

var mySQLCMFTurntableUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CMFTurntable) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cmf_turntable provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(cmfTurntableColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLCMFTurntableUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	cmfTurntableUpsertCacheMut.RLock()
	cache, cached := cmfTurntableUpsertCache[key]
	cmfTurntableUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			cmfTurntableAllColumns,
			cmfTurntableColumnsWithDefault,
			cmfTurntableColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			cmfTurntableAllColumns,
			cmfTurntablePrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert cmf_turntable, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`cmf_turntable`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `cmf_turntable` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(cmfTurntableType, cmfTurntableMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(cmfTurntableType, cmfTurntableMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for cmf_turntable")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == cmfTurntableMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(cmfTurntableType, cmfTurntableMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for cmf_turntable")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cmf_turntable")
	}

CacheNoHooks:
	if !cached {
		cmfTurntableUpsertCacheMut.Lock()
		cmfTurntableUpsertCache[key] = cache
		cmfTurntableUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single CMFTurntable record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CMFTurntable) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CMFTurntable provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cmfTurntablePrimaryKeyMapping)
	sql := "DELETE FROM `cmf_turntable` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from cmf_turntable")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for cmf_turntable")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q cmfTurntableQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no cmfTurntableQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cmf_turntable")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cmf_turntable")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CMFTurntableSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(cmfTurntableBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfTurntablePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `cmf_turntable` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfTurntablePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cmfTurntable slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cmf_turntable")
	}

	if len(cmfTurntableAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CMFTurntable) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCMFTurntable(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CMFTurntableSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CMFTurntableSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfTurntablePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `cmf_turntable`.* FROM `cmf_turntable` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfTurntablePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CMFTurntableSlice")
	}

	*o = slice

	return nil
}

// CMFTurntableExists checks if the CMFTurntable row exists.
func CMFTurntableExists(ctx context.Context, exec boil.ContextExecutor, iD uint) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `cmf_turntable` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if cmf_turntable exists")
	}

	return exists, nil
}
