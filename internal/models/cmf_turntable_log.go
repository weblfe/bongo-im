// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// CMFTurntableLog is an object representing the database table.
type CMFTurntableLog struct {
	ID      uint64 `boil:"id" json:"id" toml:"id" yaml:"id"`
	UID     int64  `boil:"uid" json:"uid" toml:"uid" yaml:"uid"`
	Liveuid int64  `boil:"liveuid" json:"liveuid" toml:"liveuid" yaml:"liveuid"`
	Showid  int64  `boil:"showid" json:"showid" toml:"showid" yaml:"showid"`
	Coin    int    `boil:"coin" json:"coin" toml:"coin" yaml:"coin"`
	Nums    int    `boil:"nums" json:"nums" toml:"nums" yaml:"nums"`
	Addtime int64  `boil:"addtime" json:"addtime" toml:"addtime" yaml:"addtime"`
	Iswin   bool   `boil:"iswin" json:"iswin" toml:"iswin" yaml:"iswin"`

	R *cmfTurntableLogR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L cmfTurntableLogL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CMFTurntableLogColumns = struct {
	ID      string
	UID     string
	Liveuid string
	Showid  string
	Coin    string
	Nums    string
	Addtime string
	Iswin   string
}{
	ID:      "id",
	UID:     "uid",
	Liveuid: "liveuid",
	Showid:  "showid",
	Coin:    "coin",
	Nums:    "nums",
	Addtime: "addtime",
	Iswin:   "iswin",
}

// Generated where

var CMFTurntableLogWhere = struct {
	ID      whereHelperuint64
	UID     whereHelperint64
	Liveuid whereHelperint64
	Showid  whereHelperint64
	Coin    whereHelperint
	Nums    whereHelperint
	Addtime whereHelperint64
	Iswin   whereHelperbool
}{
	ID:      whereHelperuint64{field: "`cmf_turntable_log`.`id`"},
	UID:     whereHelperint64{field: "`cmf_turntable_log`.`uid`"},
	Liveuid: whereHelperint64{field: "`cmf_turntable_log`.`liveuid`"},
	Showid:  whereHelperint64{field: "`cmf_turntable_log`.`showid`"},
	Coin:    whereHelperint{field: "`cmf_turntable_log`.`coin`"},
	Nums:    whereHelperint{field: "`cmf_turntable_log`.`nums`"},
	Addtime: whereHelperint64{field: "`cmf_turntable_log`.`addtime`"},
	Iswin:   whereHelperbool{field: "`cmf_turntable_log`.`iswin`"},
}

// CMFTurntableLogRels is where relationship names are stored.
var CMFTurntableLogRels = struct {
}{}

// cmfTurntableLogR is where relationships are stored.
type cmfTurntableLogR struct {
}

// NewStruct creates a new relationship struct
func (*cmfTurntableLogR) NewStruct() *cmfTurntableLogR {
	return &cmfTurntableLogR{}
}

// cmfTurntableLogL is where Load methods for each relationship are stored.
type cmfTurntableLogL struct{}

var (
	cmfTurntableLogAllColumns            = []string{"id", "uid", "liveuid", "showid", "coin", "nums", "addtime", "iswin"}
	cmfTurntableLogColumnsWithoutDefault = []string{}
	cmfTurntableLogColumnsWithDefault    = []string{"id", "uid", "liveuid", "showid", "coin", "nums", "addtime", "iswin"}
	cmfTurntableLogPrimaryKeyColumns     = []string{"id"}
)

type (
	// CMFTurntableLogSlice is an alias for a slice of pointers to CMFTurntableLog.
	// This should generally be used opposed to []CMFTurntableLog.
	CMFTurntableLogSlice []*CMFTurntableLog
	// CMFTurntableLogHook is the signature for custom CMFTurntableLog hook methods
	CMFTurntableLogHook func(context.Context, boil.ContextExecutor, *CMFTurntableLog) error

	cmfTurntableLogQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	cmfTurntableLogType                 = reflect.TypeOf(&CMFTurntableLog{})
	cmfTurntableLogMapping              = queries.MakeStructMapping(cmfTurntableLogType)
	cmfTurntableLogPrimaryKeyMapping, _ = queries.BindMapping(cmfTurntableLogType, cmfTurntableLogMapping, cmfTurntableLogPrimaryKeyColumns)
	cmfTurntableLogInsertCacheMut       sync.RWMutex
	cmfTurntableLogInsertCache          = make(map[string]insertCache)
	cmfTurntableLogUpdateCacheMut       sync.RWMutex
	cmfTurntableLogUpdateCache          = make(map[string]updateCache)
	cmfTurntableLogUpsertCacheMut       sync.RWMutex
	cmfTurntableLogUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var cmfTurntableLogBeforeInsertHooks []CMFTurntableLogHook
var cmfTurntableLogBeforeUpdateHooks []CMFTurntableLogHook
var cmfTurntableLogBeforeDeleteHooks []CMFTurntableLogHook
var cmfTurntableLogBeforeUpsertHooks []CMFTurntableLogHook

var cmfTurntableLogAfterInsertHooks []CMFTurntableLogHook
var cmfTurntableLogAfterSelectHooks []CMFTurntableLogHook
var cmfTurntableLogAfterUpdateHooks []CMFTurntableLogHook
var cmfTurntableLogAfterDeleteHooks []CMFTurntableLogHook
var cmfTurntableLogAfterUpsertHooks []CMFTurntableLogHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CMFTurntableLog) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfTurntableLogBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CMFTurntableLog) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfTurntableLogBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CMFTurntableLog) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfTurntableLogBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CMFTurntableLog) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfTurntableLogBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CMFTurntableLog) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfTurntableLogAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CMFTurntableLog) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfTurntableLogAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CMFTurntableLog) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfTurntableLogAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CMFTurntableLog) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfTurntableLogAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CMFTurntableLog) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfTurntableLogAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCMFTurntableLogHook registers your hook function for all future operations.
func AddCMFTurntableLogHook(hookPoint boil.HookPoint, cmfTurntableLogHook CMFTurntableLogHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		cmfTurntableLogBeforeInsertHooks = append(cmfTurntableLogBeforeInsertHooks, cmfTurntableLogHook)
	case boil.BeforeUpdateHook:
		cmfTurntableLogBeforeUpdateHooks = append(cmfTurntableLogBeforeUpdateHooks, cmfTurntableLogHook)
	case boil.BeforeDeleteHook:
		cmfTurntableLogBeforeDeleteHooks = append(cmfTurntableLogBeforeDeleteHooks, cmfTurntableLogHook)
	case boil.BeforeUpsertHook:
		cmfTurntableLogBeforeUpsertHooks = append(cmfTurntableLogBeforeUpsertHooks, cmfTurntableLogHook)
	case boil.AfterInsertHook:
		cmfTurntableLogAfterInsertHooks = append(cmfTurntableLogAfterInsertHooks, cmfTurntableLogHook)
	case boil.AfterSelectHook:
		cmfTurntableLogAfterSelectHooks = append(cmfTurntableLogAfterSelectHooks, cmfTurntableLogHook)
	case boil.AfterUpdateHook:
		cmfTurntableLogAfterUpdateHooks = append(cmfTurntableLogAfterUpdateHooks, cmfTurntableLogHook)
	case boil.AfterDeleteHook:
		cmfTurntableLogAfterDeleteHooks = append(cmfTurntableLogAfterDeleteHooks, cmfTurntableLogHook)
	case boil.AfterUpsertHook:
		cmfTurntableLogAfterUpsertHooks = append(cmfTurntableLogAfterUpsertHooks, cmfTurntableLogHook)
	}
}

// One returns a single cmfTurntableLog record from the query.
func (q cmfTurntableLogQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CMFTurntableLog, error) {
	o := &CMFTurntableLog{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for cmf_turntable_log")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all CMFTurntableLog records from the query.
func (q cmfTurntableLogQuery) All(ctx context.Context, exec boil.ContextExecutor) (CMFTurntableLogSlice, error) {
	var o []*CMFTurntableLog

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CMFTurntableLog slice")
	}

	if len(cmfTurntableLogAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all CMFTurntableLog records in the query.
func (q cmfTurntableLogQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count cmf_turntable_log rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q cmfTurntableLogQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if cmf_turntable_log exists")
	}

	return count > 0, nil
}

// CMFTurntableLogs retrieves all the records using an executor.
func CMFTurntableLogs(mods ...qm.QueryMod) cmfTurntableLogQuery {
	mods = append(mods, qm.From("`cmf_turntable_log`"))
	return cmfTurntableLogQuery{NewQuery(mods...)}
}

// FindCMFTurntableLog retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCMFTurntableLog(ctx context.Context, exec boil.ContextExecutor, iD uint64, selectCols ...string) (*CMFTurntableLog, error) {
	cmfTurntableLogObj := &CMFTurntableLog{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `cmf_turntable_log` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, cmfTurntableLogObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from cmf_turntable_log")
	}

	return cmfTurntableLogObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CMFTurntableLog) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cmf_turntable_log provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(cmfTurntableLogColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	cmfTurntableLogInsertCacheMut.RLock()
	cache, cached := cmfTurntableLogInsertCache[key]
	cmfTurntableLogInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			cmfTurntableLogAllColumns,
			cmfTurntableLogColumnsWithDefault,
			cmfTurntableLogColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(cmfTurntableLogType, cmfTurntableLogMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(cmfTurntableLogType, cmfTurntableLogMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `cmf_turntable_log` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `cmf_turntable_log` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `cmf_turntable_log` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, cmfTurntableLogPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into cmf_turntable_log")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == cmfTurntableLogMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cmf_turntable_log")
	}

CacheNoHooks:
	if !cached {
		cmfTurntableLogInsertCacheMut.Lock()
		cmfTurntableLogInsertCache[key] = cache
		cmfTurntableLogInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the CMFTurntableLog.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CMFTurntableLog) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	cmfTurntableLogUpdateCacheMut.RLock()
	cache, cached := cmfTurntableLogUpdateCache[key]
	cmfTurntableLogUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			cmfTurntableLogAllColumns,
			cmfTurntableLogPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update cmf_turntable_log, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `cmf_turntable_log` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, cmfTurntableLogPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(cmfTurntableLogType, cmfTurntableLogMapping, append(wl, cmfTurntableLogPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update cmf_turntable_log row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for cmf_turntable_log")
	}

	if !cached {
		cmfTurntableLogUpdateCacheMut.Lock()
		cmfTurntableLogUpdateCache[key] = cache
		cmfTurntableLogUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q cmfTurntableLogQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for cmf_turntable_log")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for cmf_turntable_log")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CMFTurntableLogSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfTurntableLogPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `cmf_turntable_log` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfTurntableLogPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in cmfTurntableLog slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all cmfTurntableLog")
	}
	return rowsAff, nil
}

var mySQLCMFTurntableLogUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CMFTurntableLog) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cmf_turntable_log provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(cmfTurntableLogColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLCMFTurntableLogUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	cmfTurntableLogUpsertCacheMut.RLock()
	cache, cached := cmfTurntableLogUpsertCache[key]
	cmfTurntableLogUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			cmfTurntableLogAllColumns,
			cmfTurntableLogColumnsWithDefault,
			cmfTurntableLogColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			cmfTurntableLogAllColumns,
			cmfTurntableLogPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert cmf_turntable_log, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`cmf_turntable_log`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `cmf_turntable_log` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(cmfTurntableLogType, cmfTurntableLogMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(cmfTurntableLogType, cmfTurntableLogMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for cmf_turntable_log")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == cmfTurntableLogMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(cmfTurntableLogType, cmfTurntableLogMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for cmf_turntable_log")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cmf_turntable_log")
	}

CacheNoHooks:
	if !cached {
		cmfTurntableLogUpsertCacheMut.Lock()
		cmfTurntableLogUpsertCache[key] = cache
		cmfTurntableLogUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single CMFTurntableLog record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CMFTurntableLog) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CMFTurntableLog provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cmfTurntableLogPrimaryKeyMapping)
	sql := "DELETE FROM `cmf_turntable_log` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from cmf_turntable_log")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for cmf_turntable_log")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q cmfTurntableLogQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no cmfTurntableLogQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cmf_turntable_log")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cmf_turntable_log")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CMFTurntableLogSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(cmfTurntableLogBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfTurntableLogPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `cmf_turntable_log` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfTurntableLogPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cmfTurntableLog slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cmf_turntable_log")
	}

	if len(cmfTurntableLogAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CMFTurntableLog) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCMFTurntableLog(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CMFTurntableLogSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CMFTurntableLogSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfTurntableLogPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `cmf_turntable_log`.* FROM `cmf_turntable_log` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfTurntableLogPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CMFTurntableLogSlice")
	}

	*o = slice

	return nil
}

// CMFTurntableLogExists checks if the CMFTurntableLog row exists.
func CMFTurntableLogExists(ctx context.Context, exec boil.ContextExecutor, iD uint64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `cmf_turntable_log` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if cmf_turntable_log exists")
	}

	return exists, nil
}
