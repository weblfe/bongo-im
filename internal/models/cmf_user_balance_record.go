// Code generated by SQLBoiler 4.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// CMFUserBalanceRecord is an object representing the database table.
type CMFUserBalanceRecord struct {
	ID      int64         `boil:"id" json:"id" toml:"id" yaml:"id"`
	UID     int64         `boil:"uid" json:"uid" toml:"uid" yaml:"uid"`
	Touid   int64         `boil:"touid" json:"touid" toml:"touid" yaml:"touid"`
	Balance types.Decimal `boil:"balance" json:"balance" toml:"balance" yaml:"balance"`
	Type    bool          `boil:"type" json:"type" toml:"type" yaml:"type"`
	Action  bool          `boil:"action" json:"action" toml:"action" yaml:"action"`
	Orderid int64         `boil:"orderid" json:"orderid" toml:"orderid" yaml:"orderid"`
	Addtime int           `boil:"addtime" json:"addtime" toml:"addtime" yaml:"addtime"`

	R *cmfUserBalanceRecordR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L cmfUserBalanceRecordL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CMFUserBalanceRecordColumns = struct {
	ID      string
	UID     string
	Touid   string
	Balance string
	Type    string
	Action  string
	Orderid string
	Addtime string
}{
	ID:      "id",
	UID:     "uid",
	Touid:   "touid",
	Balance: "balance",
	Type:    "type",
	Action:  "action",
	Orderid: "orderid",
	Addtime: "addtime",
}

// Generated where

var CMFUserBalanceRecordWhere = struct {
	ID      whereHelperint64
	UID     whereHelperint64
	Touid   whereHelperint64
	Balance whereHelpertypes_Decimal
	Type    whereHelperbool
	Action  whereHelperbool
	Orderid whereHelperint64
	Addtime whereHelperint
}{
	ID:      whereHelperint64{field: "`cmf_user_balance_record`.`id`"},
	UID:     whereHelperint64{field: "`cmf_user_balance_record`.`uid`"},
	Touid:   whereHelperint64{field: "`cmf_user_balance_record`.`touid`"},
	Balance: whereHelpertypes_Decimal{field: "`cmf_user_balance_record`.`balance`"},
	Type:    whereHelperbool{field: "`cmf_user_balance_record`.`type`"},
	Action:  whereHelperbool{field: "`cmf_user_balance_record`.`action`"},
	Orderid: whereHelperint64{field: "`cmf_user_balance_record`.`orderid`"},
	Addtime: whereHelperint{field: "`cmf_user_balance_record`.`addtime`"},
}

// CMFUserBalanceRecordRels is where relationship names are stored.
var CMFUserBalanceRecordRels = struct {
}{}

// cmfUserBalanceRecordR is where relationships are stored.
type cmfUserBalanceRecordR struct {
}

// NewStruct creates a new relationship struct
func (*cmfUserBalanceRecordR) NewStruct() *cmfUserBalanceRecordR {
	return &cmfUserBalanceRecordR{}
}

// cmfUserBalanceRecordL is where Load methods for each relationship are stored.
type cmfUserBalanceRecordL struct{}

var (
	cmfUserBalanceRecordAllColumns            = []string{"id", "uid", "touid", "balance", "type", "action", "orderid", "addtime"}
	cmfUserBalanceRecordColumnsWithoutDefault = []string{"uid", "touid"}
	cmfUserBalanceRecordColumnsWithDefault    = []string{"id", "balance", "type", "action", "orderid", "addtime"}
	cmfUserBalanceRecordPrimaryKeyColumns     = []string{"id"}
)

type (
	// CMFUserBalanceRecordSlice is an alias for a slice of pointers to CMFUserBalanceRecord.
	// This should generally be used opposed to []CMFUserBalanceRecord.
	CMFUserBalanceRecordSlice []*CMFUserBalanceRecord
	// CMFUserBalanceRecordHook is the signature for custom CMFUserBalanceRecord hook methods
	CMFUserBalanceRecordHook func(context.Context, boil.ContextExecutor, *CMFUserBalanceRecord) error

	cmfUserBalanceRecordQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	cmfUserBalanceRecordType                 = reflect.TypeOf(&CMFUserBalanceRecord{})
	cmfUserBalanceRecordMapping              = queries.MakeStructMapping(cmfUserBalanceRecordType)
	cmfUserBalanceRecordPrimaryKeyMapping, _ = queries.BindMapping(cmfUserBalanceRecordType, cmfUserBalanceRecordMapping, cmfUserBalanceRecordPrimaryKeyColumns)
	cmfUserBalanceRecordInsertCacheMut       sync.RWMutex
	cmfUserBalanceRecordInsertCache          = make(map[string]insertCache)
	cmfUserBalanceRecordUpdateCacheMut       sync.RWMutex
	cmfUserBalanceRecordUpdateCache          = make(map[string]updateCache)
	cmfUserBalanceRecordUpsertCacheMut       sync.RWMutex
	cmfUserBalanceRecordUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var cmfUserBalanceRecordBeforeInsertHooks []CMFUserBalanceRecordHook
var cmfUserBalanceRecordBeforeUpdateHooks []CMFUserBalanceRecordHook
var cmfUserBalanceRecordBeforeDeleteHooks []CMFUserBalanceRecordHook
var cmfUserBalanceRecordBeforeUpsertHooks []CMFUserBalanceRecordHook

var cmfUserBalanceRecordAfterInsertHooks []CMFUserBalanceRecordHook
var cmfUserBalanceRecordAfterSelectHooks []CMFUserBalanceRecordHook
var cmfUserBalanceRecordAfterUpdateHooks []CMFUserBalanceRecordHook
var cmfUserBalanceRecordAfterDeleteHooks []CMFUserBalanceRecordHook
var cmfUserBalanceRecordAfterUpsertHooks []CMFUserBalanceRecordHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CMFUserBalanceRecord) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfUserBalanceRecordBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CMFUserBalanceRecord) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfUserBalanceRecordBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CMFUserBalanceRecord) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfUserBalanceRecordBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CMFUserBalanceRecord) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfUserBalanceRecordBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CMFUserBalanceRecord) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfUserBalanceRecordAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CMFUserBalanceRecord) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfUserBalanceRecordAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CMFUserBalanceRecord) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfUserBalanceRecordAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CMFUserBalanceRecord) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfUserBalanceRecordAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CMFUserBalanceRecord) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range cmfUserBalanceRecordAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCMFUserBalanceRecordHook registers your hook function for all future operations.
func AddCMFUserBalanceRecordHook(hookPoint boil.HookPoint, cmfUserBalanceRecordHook CMFUserBalanceRecordHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		cmfUserBalanceRecordBeforeInsertHooks = append(cmfUserBalanceRecordBeforeInsertHooks, cmfUserBalanceRecordHook)
	case boil.BeforeUpdateHook:
		cmfUserBalanceRecordBeforeUpdateHooks = append(cmfUserBalanceRecordBeforeUpdateHooks, cmfUserBalanceRecordHook)
	case boil.BeforeDeleteHook:
		cmfUserBalanceRecordBeforeDeleteHooks = append(cmfUserBalanceRecordBeforeDeleteHooks, cmfUserBalanceRecordHook)
	case boil.BeforeUpsertHook:
		cmfUserBalanceRecordBeforeUpsertHooks = append(cmfUserBalanceRecordBeforeUpsertHooks, cmfUserBalanceRecordHook)
	case boil.AfterInsertHook:
		cmfUserBalanceRecordAfterInsertHooks = append(cmfUserBalanceRecordAfterInsertHooks, cmfUserBalanceRecordHook)
	case boil.AfterSelectHook:
		cmfUserBalanceRecordAfterSelectHooks = append(cmfUserBalanceRecordAfterSelectHooks, cmfUserBalanceRecordHook)
	case boil.AfterUpdateHook:
		cmfUserBalanceRecordAfterUpdateHooks = append(cmfUserBalanceRecordAfterUpdateHooks, cmfUserBalanceRecordHook)
	case boil.AfterDeleteHook:
		cmfUserBalanceRecordAfterDeleteHooks = append(cmfUserBalanceRecordAfterDeleteHooks, cmfUserBalanceRecordHook)
	case boil.AfterUpsertHook:
		cmfUserBalanceRecordAfterUpsertHooks = append(cmfUserBalanceRecordAfterUpsertHooks, cmfUserBalanceRecordHook)
	}
}

// One returns a single cmfUserBalanceRecord record from the query.
func (q cmfUserBalanceRecordQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CMFUserBalanceRecord, error) {
	o := &CMFUserBalanceRecord{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for cmf_user_balance_record")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all CMFUserBalanceRecord records from the query.
func (q cmfUserBalanceRecordQuery) All(ctx context.Context, exec boil.ContextExecutor) (CMFUserBalanceRecordSlice, error) {
	var o []*CMFUserBalanceRecord

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CMFUserBalanceRecord slice")
	}

	if len(cmfUserBalanceRecordAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all CMFUserBalanceRecord records in the query.
func (q cmfUserBalanceRecordQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count cmf_user_balance_record rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q cmfUserBalanceRecordQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if cmf_user_balance_record exists")
	}

	return count > 0, nil
}

// CMFUserBalanceRecords retrieves all the records using an executor.
func CMFUserBalanceRecords(mods ...qm.QueryMod) cmfUserBalanceRecordQuery {
	mods = append(mods, qm.From("`cmf_user_balance_record`"))
	return cmfUserBalanceRecordQuery{NewQuery(mods...)}
}

// FindCMFUserBalanceRecord retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCMFUserBalanceRecord(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*CMFUserBalanceRecord, error) {
	cmfUserBalanceRecordObj := &CMFUserBalanceRecord{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `cmf_user_balance_record` where `id`=?", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, cmfUserBalanceRecordObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from cmf_user_balance_record")
	}

	return cmfUserBalanceRecordObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CMFUserBalanceRecord) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cmf_user_balance_record provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(cmfUserBalanceRecordColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	cmfUserBalanceRecordInsertCacheMut.RLock()
	cache, cached := cmfUserBalanceRecordInsertCache[key]
	cmfUserBalanceRecordInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			cmfUserBalanceRecordAllColumns,
			cmfUserBalanceRecordColumnsWithDefault,
			cmfUserBalanceRecordColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(cmfUserBalanceRecordType, cmfUserBalanceRecordMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(cmfUserBalanceRecordType, cmfUserBalanceRecordMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `cmf_user_balance_record` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `cmf_user_balance_record` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `cmf_user_balance_record` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, cmfUserBalanceRecordPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into cmf_user_balance_record")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == cmfUserBalanceRecordMapping["id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.ID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cmf_user_balance_record")
	}

CacheNoHooks:
	if !cached {
		cmfUserBalanceRecordInsertCacheMut.Lock()
		cmfUserBalanceRecordInsertCache[key] = cache
		cmfUserBalanceRecordInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the CMFUserBalanceRecord.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CMFUserBalanceRecord) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	cmfUserBalanceRecordUpdateCacheMut.RLock()
	cache, cached := cmfUserBalanceRecordUpdateCache[key]
	cmfUserBalanceRecordUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			cmfUserBalanceRecordAllColumns,
			cmfUserBalanceRecordPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update cmf_user_balance_record, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `cmf_user_balance_record` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, cmfUserBalanceRecordPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(cmfUserBalanceRecordType, cmfUserBalanceRecordMapping, append(wl, cmfUserBalanceRecordPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update cmf_user_balance_record row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for cmf_user_balance_record")
	}

	if !cached {
		cmfUserBalanceRecordUpdateCacheMut.Lock()
		cmfUserBalanceRecordUpdateCache[key] = cache
		cmfUserBalanceRecordUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q cmfUserBalanceRecordQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for cmf_user_balance_record")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for cmf_user_balance_record")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CMFUserBalanceRecordSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfUserBalanceRecordPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `cmf_user_balance_record` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfUserBalanceRecordPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in cmfUserBalanceRecord slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all cmfUserBalanceRecord")
	}
	return rowsAff, nil
}

var mySQLCMFUserBalanceRecordUniqueColumns = []string{
	"id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CMFUserBalanceRecord) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no cmf_user_balance_record provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(cmfUserBalanceRecordColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLCMFUserBalanceRecordUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	cmfUserBalanceRecordUpsertCacheMut.RLock()
	cache, cached := cmfUserBalanceRecordUpsertCache[key]
	cmfUserBalanceRecordUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			cmfUserBalanceRecordAllColumns,
			cmfUserBalanceRecordColumnsWithDefault,
			cmfUserBalanceRecordColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			cmfUserBalanceRecordAllColumns,
			cmfUserBalanceRecordPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert cmf_user_balance_record, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`cmf_user_balance_record`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `cmf_user_balance_record` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(cmfUserBalanceRecordType, cmfUserBalanceRecordMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(cmfUserBalanceRecordType, cmfUserBalanceRecordMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for cmf_user_balance_record")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.ID = int64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == cmfUserBalanceRecordMapping["id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(cmfUserBalanceRecordType, cmfUserBalanceRecordMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for cmf_user_balance_record")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for cmf_user_balance_record")
	}

CacheNoHooks:
	if !cached {
		cmfUserBalanceRecordUpsertCacheMut.Lock()
		cmfUserBalanceRecordUpsertCache[key] = cache
		cmfUserBalanceRecordUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single CMFUserBalanceRecord record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CMFUserBalanceRecord) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CMFUserBalanceRecord provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cmfUserBalanceRecordPrimaryKeyMapping)
	sql := "DELETE FROM `cmf_user_balance_record` WHERE `id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from cmf_user_balance_record")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for cmf_user_balance_record")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q cmfUserBalanceRecordQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no cmfUserBalanceRecordQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cmf_user_balance_record")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cmf_user_balance_record")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CMFUserBalanceRecordSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(cmfUserBalanceRecordBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfUserBalanceRecordPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `cmf_user_balance_record` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfUserBalanceRecordPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cmfUserBalanceRecord slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for cmf_user_balance_record")
	}

	if len(cmfUserBalanceRecordAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CMFUserBalanceRecord) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCMFUserBalanceRecord(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CMFUserBalanceRecordSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CMFUserBalanceRecordSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cmfUserBalanceRecordPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `cmf_user_balance_record`.* FROM `cmf_user_balance_record` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, cmfUserBalanceRecordPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CMFUserBalanceRecordSlice")
	}

	*o = slice

	return nil
}

// CMFUserBalanceRecordExists checks if the CMFUserBalanceRecord row exists.
func CMFUserBalanceRecordExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `cmf_user_balance_record` where `id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if cmf_user_balance_record exists")
	}

	return exists, nil
}
